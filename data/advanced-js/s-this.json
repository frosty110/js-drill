{
  "id": "s-this",
  "title": "this / bind / call / apply",
  "section": "Advanced JS",
  "track": "syntax",
  "status": "full",
  "description": "this is decided by how a function is CALLED. Four call patterns (method, standalone, new, explicit) + the arrow gotcha (lexical capture).",
  "mechanics": [],
  "reference": {
    "code": "const user = {\n  name: \"Ada\",\n  greet() { return \"Hi \" + this.name; }      // method call → this = user\n};\n\nuser.greet();                                // \"Hi Ada\"\n\nconst detached = user.greet;\ndetached();                                  // standalone → this is undefined (strict)\n\n// Fix 1: bind returns a NEW function with this locked\nconst bound = user.greet.bind(user);\nbound();                                     // \"Hi Ada\"\n\n// Fix 2: call / apply invoke once with an explicit this\nfunction intro(greet, punct) {\n  return greet + \" \" + this.name + punct;\n}\nintro.call(user, \"Hello\", \"!\");              // \"Hello Ada!\"\nintro.apply(user, [\"Hey\", \".\"]);             // \"Hey Ada.\" — array form\n\n// Arrow functions IGNORE call/bind — they capture this lexically\nconst obj = {\n  name: \"Lee\",\n  greet() { return (() => this.name)(); }    // arrow → this = obj\n};",
    "notes": [
      "this is bound at CALL time for normal functions; arrow functions capture this at DEFINITION time.",
      "bind returns a new function; call/apply invoke immediately. apply takes args as an array.",
      "Passing a method as a callback (setTimeout, map) detaches it — wrap in arrow or bind to preserve this."
    ]
  },
  "L1": {
    "questions": [
      {
        "q": "When is `this` resolved for a regular (non-arrow) function?",
        "options": [
          "Lexically — captured from the enclosing scope at definition",
          "When the function is called — based on how it is called",
          "Always the global object in strict mode",
          "Whichever object owns the variable that holds the function"
        ],
        "answer": 1
      },
      {
        "q": "Difference between call and apply?",
        "options": [
          "call binds permanently, apply does not",
          "apply takes args as an array; call takes them comma-separated",
          "apply spreads array args; call also accepts an array but spreads it the same way",
          "apply ignores the first argument (thisArg); call uses it"
        ],
        "answer": 1
      },
      {
        "q": "What does bind return?",
        "options": [
          "The result of calling the function with the given this",
          "A Proxy that intercepts calls and substitutes this",
          "A new function with this (and optional args) locked in",
          "The original function mutated in place"
        ],
        "answer": 2
      },
      {
        "q": "Inside `obj.greet() { return (() => this.name)(); }`, the arrow’s `this` is:",
        "options": [
          "undefined — the IIFE is a standalone call, so this is unbound",
          "The global object — the arrow is invoked without a receiver",
          "obj — arrows inherit this from the enclosing scope",
          "The arrow function object itself, since arrows self-bind"
        ],
        "answer": 2
      }
    ]
  },
  "L2": {
    "exercises": [
      {
        "prompt": "Fix the lost-this problem by binding the detached method.",
        "template": "const user = {\n  name: \"Ada\",\n  greet() { ___ \"Hi \" + ___.name; }\n};\nconst detached = user.greet;\nconst bound = detached.___(user);\nconsole.log(___());",
        "blanks": [
          {
            "answer": "return",
            "hint": "ship the greeting out"
          },
          {
            "answer": "this",
            "hint": "method-call receiver"
          },
          {
            "answer": "bind",
            "hint": "returns a new function with this locked"
          },
          {
            "answer": "bound",
            "hint": "the bound function — call it"
          }
        ],
        "expectedOutput": "Hi Ada"
      },
      {
        "prompt": "Invoke intro once with call, then with apply (note the array).",
        "template": "function intro(greet, punct) {\n  return greet + \" \" + ___.name + punct;\n}\nconst u = { name: \"Lee\" };\nconst a = intro.___(u, \"Hello\", \"!\");\nconst b = intro.___(u, ___\"Hey\", \".\"___);\nconsole.log(a);\nconsole.log(b);",
        "blanks": [
          {
            "answer": "this",
            "hint": "filled in by call/apply"
          },
          {
            "answer": "call",
            "hint": "comma-separated args"
          },
          {
            "answer": "apply",
            "hint": "args as an array"
          },
          {
            "answer": "[",
            "hint": "apply takes an args array — open bracket"
          },
          {
            "answer": "]",
            "hint": "close the args array"
          }
        ],
        "expectedOutput": "Hello Lee!\nHey Lee."
      }
    ]
  },
  "L3": {
    "prompt": "Inside `obj.greet()`, declare an arrow function that reads `this.name`, then return its call. Log `obj.greet()` for `{ name: \"Ada\" }`.",
    "expectedOutput": "Ada",
    "canonical": "const obj = {\n  name: \"Ada\",\n  greet() {\n    const arrow = () => this.name;\n    return arrow();\n  }\n};\nconsole.log(obj.greet());",
    "hints": [
      "Arrow functions do not have their own this — they inherit it from the enclosing method.",
      "Inside greet(), this is obj because of the method call."
    ]
  }
}
