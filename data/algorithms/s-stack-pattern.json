{
  "id": "s-stack-pattern",
  "title": "Array-as-stack",
  "section": "Algorithms",
  "track": "patterns",
  "status": "full",
  "description": "A plain array becomes a stack with push + pop on the same end — LIFO, both O(1).",
  "mechanics": ["array-as-stack"],
  "reference": {
    "code": "// Stack = LIFO (Last In, First Out)\nconst stack = [];\n\nstack.push(1);     // [1]\nstack.push(2);     // [1, 2]\nstack.push(3);     // [1, 2, 3]\n\nstack.pop();       // 3 → stack is [1, 2]\nstack.pop();       // 2 → stack is [1]\n\n// Peek without removing\nstack[stack.length - 1];  // 1\nstack.at(-1);             // 1 (cleaner)\n\n// Empty check\nwhile (stack.length) { /* drain */ }\n\n// Classic uses:\n//   - Bracket matching:  push opens, pop on close, compare pair\n//   - Backtracking / DFS: push frame, pop when done\n//   - Undo history:       push state, pop to revert",
    "notes": [
      "Both push and pop are O(1) amortized — the fastest array pattern.",
      "Use stack.length as the truthy \"non-empty\" check; stack.at(-1) to peek.",
      "When the problem says \"most recent\", \"matching pairs\", or \"undo\", reach for a stack."
    ]
  },
  "L1": {
    "questions": [
      {
        "q": "Which pair of methods turns an array into a stack?",
        "options": [
          "push / shift",
          "push / pop",
          "unshift / pop",
          "shift / unshift"
        ],
        "answer": 1
      },
      {
        "q": "A stack is which ordering?",
        "options": [
          "FIFO — first in, first out",
          "LIFO — last in, first out",
          "Sorted ascending",
          "Random"
        ],
        "answer": 1
      },
      {
        "q": "Time complexity of stack.push and stack.pop?",
        "options": [
          "O(n) each",
          "O(log n) each",
          "O(1) amortized each",
          "push is O(1), pop is O(n)"
        ],
        "answer": 2
      },
      {
        "q": "Which problem is the most natural fit for a stack?",
        "options": [
          "Level-order tree traversal",
          "Matching balanced brackets",
          "Counting unique elements",
          "Sorting numbers"
        ],
        "answer": 1
      }
    ]
  },
  "L2": {
    "exercises": [
      {
        "prompt": "Push 1, 2, 3 onto a stack, then pop twice and log both results.",
        "template": "const stack = [];\nstack.___(1);\nstack.___(2);\nstack.___(3);\nconsole.log(stack.___(), stack.___());",
        "blanks": [
          {
            "answer": "push",
            "hint": "add to top"
          },
          {
            "answer": "push"
          },
          {
            "answer": "push"
          },
          {
            "answer": "pop",
            "hint": "remove top — returns the item"
          },
          {
            "answer": "pop"
          }
        ],
        "expectedOutput": "3 2"
      },
      {
        "prompt": "Use a stack to check whether brackets are balanced.",
        "template": "function isBalanced(s) {\n  const stack = [];\n  const pairs = { \")\": \"(\", \"]\": \"[\", \"}\": \"{\" };\n  for (const ch of s) {\n    if (ch === \"(\" || ch === \"[\" || ch === \"{\") stack.___(ch);\n    else if (stack.___() !== pairs[ch]) return false;\n  }\n  return stack.length === 0;\n}\nconsole.log(isBalanced(\"([])\"));",
        "blanks": [
          {
            "answer": "push",
            "hint": "opening bracket goes on the stack"
          },
          {
            "answer": "pop",
            "hint": "closing bracket must match the top"
          }
        ],
        "expectedOutput": "true"
      }
    ]
  },
  "L3": {
    "prompt": "Write `reverseString(s)` using a stack: push every char, then pop them back into a result string. Log `reverseString(\"hello\")`.",
    "expectedOutput": "olleh",
    "canonical": "function reverseString(s) {\n  const stack = [];\n  for (const ch of s) stack.push(ch);\n  let out = \"\";\n  while (stack.length) out += stack.pop();\n  return out;\n}\nconsole.log(reverseString(\"hello\"));",
    "hints": [
      "Push every char onto an array.",
      "Drain with pop while stack.length is truthy."
    ]
  }
}
