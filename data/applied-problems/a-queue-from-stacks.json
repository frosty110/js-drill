{
  "id": "a-queue-from-stacks",
  "title": "Queue Using Two Stacks",
  "section": "Applied Problems",
  "track": "applied",
  "status": "full",
  "description": "FIFO queue built from two LIFO stacks (just JS arrays). push goes to `in`. pop/peek read from `out`; when `out` is empty, drain ALL of `in` into `out` by popping — which reverses the order, putting the oldest element on top. Each element migrates at most once, so the work is amortized O(1) per op.",
  "reference": {
    "code": "class Queue {\n  constructor() { this.in = []; this.out = []; }\n  push(v) { this.in.push(v); }              // newest goes here\n  _shift() {                                // drain in -> out only when out is empty\n    if (!this.out.length) {\n      while (this.in.length) this.out.push(this.in.pop());\n    }\n  }\n  pop() { this._shift(); return this.out.pop(); }    // oldest is on top of out\n  peek() { this._shift(); return this.out[this.out.length - 1]; }\n  get size() { return this.in.length + this.out.length; }\n}\n// Amortized O(1) push/pop/peek, Space O(n)",
    "notes": [
      "Only drain when `out` is empty. Draining on every pop ruins the amortization and produces wrong order.",
      "Pop from `in` and push to `out` — this reversal turns LIFO order into FIFO order.",
      "size combines both stacks. Forgetting one half makes the queue look smaller than it is."
    ]
  },
  "L1": {
    "questions": [
      {
        "q": "Why two stacks instead of one?",
        "options": [
          "Memory layout",
          "A single stack cannot give FIFO order; reversing one stack into another flips LIFO to FIFO",
          "It is required by ES6",
          "Performance"
        ],
        "answer": 1
      },
      {
        "q": "When should the in-stack be drained into the out-stack?",
        "options": [
          "On every push",
          "On every pop",
          "Only when out is empty and a pop/peek is requested",
          "Never — keep them separate forever"
        ],
        "answer": 2
      },
      {
        "q": "What is the amortized cost of pop?",
        "options": [
          "O(n) worst case is the only measure that matters",
          "O(log n)",
          "O(1) — each element moves between stacks at most once",
          "O(n) amortized"
        ],
        "answer": 2
      },
      {
        "q": "After push(1), push(2), push(3), what does pop return?",
        "options": [
          "3 — LIFO",
          "1 — FIFO",
          "2",
          "undefined"
        ],
        "answer": 1
      }
    ]
  },
  "L2": {
    "exercises": [
      {
        "prompt": "Fill in _shift and pop so the queue returns FIFO order.",
        "template": "class Queue {\n  constructor() { this.in = []; this.out = []; }\n  push(v) { this.in.push(v); }\n  _shift() {\n    if (!this.out.length) {\n      while (this.in.length) this.out.push(this.in.___());\n    }\n  }\n  pop() { this._shift(); return this.out.___(); }\n}\nconst q = new Queue();\nq.push(1); q.push(2); q.push(3);\nconsole.log(q.pop());\nconsole.log(q.pop());\nconsole.log(q.pop());",
        "blanks": [
          { "answer": "pop", "hint": "drain LIFO from in" },
          { "answer": "pop", "hint": "top of out is the oldest element after reversal" }
        ],
        "expectedOutput": "1\n2\n3"
      }
    ]
  },
  "L3": {
    "prompt": "Build Queue with push, pop, peek, size. push 1,2,3; log peek, pop; push 4; log pop, peek, pop, pop, size.",
    "expectedOutput": "1\n1\n2\n3\n3\n4\n0",
    "canonical": "class Queue {\n  constructor() { this.in = []; this.out = []; }\n  push(v) { this.in.push(v); }\n  _shift() {\n    if (!this.out.length) {\n      while (this.in.length) this.out.push(this.in.pop());\n    }\n  }\n  pop() { this._shift(); return this.out.pop(); }\n  peek() { this._shift(); return this.out[this.out.length - 1]; }\n  get size() { return this.in.length + this.out.length; }\n}\nconst q = new Queue();\nq.push(1); q.push(2); q.push(3);\nconsole.log(q.peek());\nconsole.log(q.pop());\nq.push(4);\nconsole.log(q.pop());\nconsole.log(q.peek());\nconsole.log(q.pop());\nconsole.log(q.pop());\nconsole.log(q.size);",
    "hints": [
      "Pushes are cheap — always append to `in`.",
      "On read, refill `out` from `in` ONLY when `out` is empty — that is what amortizes pop to O(1).",
      "Top of `out` (after reversal) is the oldest element."
    ]
  }
}
