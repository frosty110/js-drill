{
  "id": "s-arr-mutate",
  "title": "push / pop / splice",
  "section": "Arrays",
  "track": "syntax",
  "status": "full",
  "description": "The mutating array methods — push, pop, shift, unshift, splice — and what each returns vs. mutates.",
  "mechanics": ["array-as-stack", "array-as-queue"],
  "reference": {
    "code": "// END of array (fast)\narr.push(x);        // appends, returns NEW length\narr.pop();          // removes last, returns the removed item (or undefined)\n\n// FRONT of array (slow, O(n) — re-indexes everything)\narr.unshift(x);     // prepends, returns NEW length\narr.shift();        // removes first, returns the removed item\n\n// splice — surgical insert/remove ANYWHERE (mutates, returns removed)\narr.splice(start, deleteCount, ...itemsToInsert);\n[1,2,3,4].splice(1, 2);          // returns [2,3], arr becomes [1,4]\n[1,2,3].splice(1, 0, \"a\", \"b\");  // returns [],   arr becomes [1,\"a\",\"b\",2,3]",
    "notes": [
      "push/unshift return the NEW LENGTH; pop/shift/splice return the REMOVED items.",
      "shift and unshift are O(n) — they re-index every remaining element. Prefer push/pop when order does not matter.",
      "splice MUTATES. The non-mutating cousin is `toSpliced` (ES2023) or `[...arr.slice(0,i), ...arr.slice(j)]`."
    ]
  },
  "L1": {
    "questions": [
      {
        "q": "What does arr.push(x) return?",
        "options": [
          "The pushed item",
          "The new array length",
          "undefined",
          "The original array"
        ],
        "answer": 1
      },
      {
        "q": "What does arr.pop() return?",
        "options": [
          "The new length",
          "The removed item",
          "undefined always",
          "A boolean"
        ],
        "answer": 1
      },
      {
        "q": "Why is unshift slower than push on large arrays?",
        "options": [
          "Engines optimize push only",
          "unshift re-indexes every element",
          "unshift allocates more memory",
          "It is not slower"
        ],
        "answer": 1,
        "explain": "Front operations shift every remaining index by one — O(n)."
      },
      {
        "q": "splice(1, 2) on [1,2,3,4] — what does it RETURN?",
        "options": [
          "[1, 4]",
          "[2, 3]",
          "2",
          "undefined"
        ],
        "answer": 1,
        "explain": "splice returns the removed slice; the original mutates to [1, 4]."
      }
    ]
  },
  "L2": {
    "exercises": [
      {
        "prompt": "Push 4 onto the array and log the return value plus the new length.",
        "template": "const a = [1, 2, 3];\nconst r = a.___(4);\nconsole.log(r, a.length);",
        "blanks": [
          {
            "answer": "push",
            "hint": "append to end"
          }
        ],
        "expectedOutput": "4 4"
      },
      {
        "prompt": "Splice two items out starting at index 1; log removed and the remaining array.",
        "template": "const a = [1, 2, 3, 4];\nconst removed = a.___(1, 2);\nconsole.log(JSON.stringify(removed), JSON.stringify(a));",
        "blanks": [
          {
            "answer": "splice",
            "hint": "surgical remove/insert"
          }
        ],
        "expectedOutput": "[2,3] [1,4]"
      }
    ]
  },
  "L3": {
    "prompt": "Write `rotateLeft(arr)` that moves the first element to the end (mutating) and returns the array. Log `JSON.stringify(rotateLeft([1, 2, 3, 4]))`.",
    "expectedOutput": "[2,3,4,1]",
    "canonical": "function rotateLeft(arr) {\n  const first = arr.shift();\n  arr.push(first);\n  return arr;\n}\nconsole.log(JSON.stringify(rotateLeft([1, 2, 3, 4])));",
    "hints": [
      "shift removes and returns the first item.",
      "push that item to the end."
    ]
  }
}
