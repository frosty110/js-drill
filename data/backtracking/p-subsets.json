{
  "id": "p-subsets",
  "title": "Subsets",
  "section": "Backtracking",
  "track": "patterns",
  "status": "full",
  "description": "Backtracking: build every subset by deciding include-or-skip at each index. 2^n results.",
  "mechanics": ["backtrack-template"],
  "reference": {
    "code": "function subsets(nums) {\n  const res = [];\n  function backtrack(start, path) {\n    res.push([...path]);              // every node is a valid subset\n    for (let i = start; i < nums.length; i++) {\n      path.push(nums[i]);              // choose\n      backtrack(i + 1, path);          // explore (i+1 prevents reuse)\n      path.pop();                      // un-choose\n    }\n  }\n  backtrack(0, []);\n  return res;\n}\n// Time O(n * 2^n), Space O(n) recursion + output",
    "notes": [
      "Push a COPY (`[...path]`) — the same `path` array is mutated across recursive calls.",
      "Use `start` (not 0) so each element is only considered once per branch.",
      "Every recursion node is a valid subset — record before the loop, not at a base case."
    ]
  },
  "L1": {
    "questions": [
      {
        "q": "Why does the inner loop start at `start` rather than 0?",
        "options": [
          "Performance only",
          "To prevent revisiting earlier elements and producing duplicates",
          "Required by JS",
          "To skip the empty subset"
        ],
        "answer": 1
      },
      {
        "q": "Why push `[...path]` instead of `path` into `res`?",
        "options": [
          "Style preference",
          "Because `path` is mutated by later push/pop calls",
          "Arrays cannot be stored otherwise",
          "To save memory"
        ],
        "answer": 1
      },
      {
        "q": "How many subsets does an array of length n produce?",
        "options": [
          "n",
          "n!",
          "2^n",
          "n^2"
        ],
        "answer": 2
      },
      {
        "q": "When is a partial `path` recorded as a result?",
        "options": [
          "Only when full length",
          "At every recursive call",
          "Only at leaves",
          "Only when sorted"
        ],
        "answer": 1
      }
    ]
  },
  "L2": {
    "exercises": [
      {
        "prompt": "Fill in the include/exclude backtracking subsets generator.",
        "template": "function subsets(nums) {\n  const res = [];\n  function backtrack(start, path) {\n    res.push([...___]);\n    for (let i = ___; i < nums.length; i++) {\n      path.push(nums[i]);\n      backtrack(i + ___, path);\n      path.___();\n    }\n  }\n  backtrack(0, []);\n  return res;\n}\nconsole.log(JSON.stringify(subsets([1, 2, 3])));",
        "blanks": [
          {
            "answer": "path",
            "hint": "snapshot the current path"
          },
          {
            "answer": "start",
            "hint": "avoid revisiting earlier indices"
          },
          {
            "answer": "1",
            "hint": "advance past the chosen index"
          },
          {
            "answer": "pop",
            "hint": "un-choose before next iteration"
          }
        ],
        "expectedOutput": "[[],[1],[1,2],[1,2,3],[1,3],[2],[2,3],[3]]"
      }
    ]
  },
  "L3": {
    "prompt": "Write `subsets(nums)` returning every subset (including the empty set) via backtracking. Log `JSON.stringify(subsets([1, 2, 3]))`.",
    "expectedOutput": "[[],[1],[1,2],[1,2,3],[1,3],[2],[2,3],[3]]",
    "canonical": "function subsets(nums) {\n  const res = [];\n  function backtrack(start, path) {\n    res.push([...path]);\n    for (let i = start; i < nums.length; i++) {\n      path.push(nums[i]);\n      backtrack(i + 1, path);\n      path.pop();\n    }\n  }\n  backtrack(0, []);\n  return res;\n}\nconsole.log(JSON.stringify(subsets([1, 2, 3])));",
    "hints": [
      "Record the path at the TOP of every recursive call.",
      "Push a copy of `path`; the array is reused across recursion."
    ]
  }
}
