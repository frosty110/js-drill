{
  "id": "s-strings",
  "title": "String basics",
  "section": "Basics",
  "track": "syntax",
  "status": "full",
  "description": "Strings are immutable primitives — make them with literals or String(); read with .length and [i]; iterate with for...of.",
  "mechanics": [],
  "reference": {
    "code": "// Two ways to make a string\nconst a = \"hello\";           // literal — preferred\nconst b = String(123);       // \"123\"  — coerces anything to string\ntypeof a;                    // \"string\"  (primitive)\n\n// length and index access\n\"hello\".length;              // 5\n\"hello\"[0];                  // \"h\"\n\"hello\"[10];                 // undefined  (no out-of-bounds error)\n\n// Iteration with for...of yields each character\nlet out = \"\";\nfor (const ch of \"abc\") out += ch + \"-\";\n// out === \"a-b-c-\"\n\n// Immutability — methods return NEW strings; the original is unchanged\nconst s = \"hi\";\ns.toUpperCase();             // \"HI\"   — s is still \"hi\"\n// s[0] = \"X\";               // silently fails (TypeError in strict mode)\n\n// Primitive vs wrapper — avoid new String()\nconst w = new String(\"hi\");  // object wrapper, typeof \"object\"",
    "notes": [
      "Prefer string literals over new String() — the wrapper object breaks === comparisons and has typeof \"object\".",
      "Strings are immutable: s[0] = \"X\" throws a TypeError in strict mode (and silently does nothing in sloppy mode) — every transform returns a fresh string instead.",
      "Out-of-bounds indexing returns undefined, not an error — guard with .length when iterating manually."
    ]
  },
  "L1": {
    "questions": [
      {
        "q": "What does `new String(\"hi\") === \"hi\"` return?",
        "options": [
          "true — both are the string 'hi'",
          "false — the wrapper object is not the primitive",
          "true — `===` unwraps the String object before comparing",
          "false — but `new String(\"hi\") == \"hi\"` is also false"
        ],
        "answer": 1,
        "explain": "new String() creates an object wrapper; typeof is 'object'. Reference-equality against the primitive 'hi' is always false. Avoid new String()."
      },
      {
        "q": "What does `\"hello\"[10]` return?",
        "options": [
          "\"\"",
          "undefined",
          "It throws RangeError",
          "null"
        ],
        "answer": 1,
        "explain": "Out-of-bounds string indexing yields undefined — not an empty string, and no throw. Guard with .length when you walk a string by index."
      },
      {
        "q": "After `const s = \"hi\"; s.toUpperCase();` what is s?",
        "options": [
          "\"HI\"",
          "\"hi\"",
          "undefined — the return value was discarded",
          "\"HI\" — `.toUpperCase()` mutates in place when the receiver is a let/const"
        ],
        "answer": 1,
        "explain": "Strings are immutable, so toUpperCase returns a NEW string and here the return value is thrown away — s never changes. The declaration keyword controls rebinding only; a let-declared string is just as immutable."
      },
      {
        "q": "Which loop iterates the CHARACTERS of a string?",
        "options": [
          "for (const ch in s) — yields each character",
          "for (const ch of s) — yields each character",
          "s.forEach(ch => ...) — works on strings too",
          "for (let i = 0; i <= s.length; i++) — bracket-access in body"
        ],
        "answer": 1,
        "explain": "for-in returns string KEYS ('0','1',...), not values. forEach is array-only. The for-loop has an off-by-one (<= instead of <). for...of is the canonical char iterator."
      }
    ]
  },
  "L2": {
    "exercises": [
      {
        "prompt": "Demonstrate immutability — assigning to s[0] THROWS in strict mode (which is how this drill runs your code, and how every module and class body runs); .toUpperCase() returns a NEW string.",
        "template": "const s = \"hi\";\ntry { s[___] = \"X\"; } catch { console.log(\"TypeError\"); }\nconst upper = s.___();\nconsole.log(s, upper, ___ s, s.___);",
        "blanks": [
          {
            "answer": "0",
            "hint": "first index"
          },
          {
            "answer": "toUpperCase",
            "hint": "the method that returns 'HI' from 'hi' — string method, no args"
          },
          {
            "answer": "typeof",
            "hint": "one-word operator returning the type tag"
          },
          {
            "answer": "length",
            "hint": "string size property"
          }
        ],
        "expectedOutput": "TypeError\nhi HI string 2"
      },
      {
        "prompt": "Index the first char and iterate with for...of.",
        "template": "const s = \"cat\";\nlet out = \"\";\n___ (___ ch ___ s) out ___ ch + \"-\";\nconsole.log(s[___], out);",
        "blanks": [
          {
            "answer": "for",
            "hint": "loop keyword"
          },
          {
            "answer": "const",
            "hint": "block-scoped binding for the iteration var"
          },
          {
            "answer": "of",
            "hint": "for...of yields values"
          },
          {
            "answer": "+=",
            "hint": "append to running string"
          },
          {
            "answer": "0",
            "hint": "first index"
          }
        ],
        "expectedOutput": "c c-a-t-"
      }
    ]
  },
  "L3": {
    "prompt": "Write `firstChar(s)` returning the first character of s. Log `firstChar(\"hello\")`, `firstChar(\"Z\")`, and `typeof \"x\" === \"string\"`.",
    "expectedOutput": "h Z true",
    "canonical": "function firstChar(s) {\n  return s[0];\n}\nconsole.log(firstChar(\"hello\"), firstChar(\"Z\"), typeof \"x\" === \"string\");",
    "hints": [
      "Index with [0].",
      "typeof on a string literal is \"string\"."
    ]
  }
}
