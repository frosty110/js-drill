{
  "_meta": "iter 79: curated complexity-claim drills for Smell-test mode. Each entry pairs a lesson with its actual time complexity + a plausible-but-wrong distractor. The session randomly shows actual OR distractor as the 'claim'; user picks correct/wrong + sees the actual either way. Trains the interview reflex 'does the stated complexity match the code?' — graded heavily but never trained in isolation.",
  "claims": {
    "two-sum": {
      "actual": "O(n)",
      "distractor": "O(n²)",
      "note": "Single pass with a hash map; complement check is O(1) amortized."
    },
    "valid-palindrome": {
      "actual": "O(n)",
      "distractor": "O(n log n)",
      "note": "Two pointers walk the string once; alphanumeric filter is per-char O(1)."
    },
    "p-bfs": {
      "actual": "O(n)",
      "distractor": "O(n²)",
      "note": "Each node enqueued once, processed once."
    },
    "binary-search": {
      "actual": "O(log n)",
      "distractor": "O(n)",
      "note": "Each iteration halves the search space."
    },
    "p-3sum": {
      "actual": "O(n²)",
      "distractor": "O(n log n)",
      "note": "Sort is O(n log n); the outer loop × inner two-pointer pass is O(n²)."
    },
    "p-merge-intervals": {
      "actual": "O(n log n)",
      "distractor": "O(n)",
      "note": "Sort dominates; the merge pass after sorting is O(n)."
    },
    "p-min-window": {
      "actual": "O(n)",
      "distractor": "O(n²)",
      "note": "Each character entered and exited the window at most once; amortized linear."
    },
    "valid-parentheses": {
      "actual": "O(n)",
      "distractor": "O(n²)",
      "note": "Single pass over the string with O(1) stack push/pop per char."
    },
    "p-contains-dup": {
      "actual": "O(n)",
      "distractor": "O(n²)",
      "note": "Hash-set seen-check is O(1) amortized; single linear pass."
    },
    "p-anagrams": {
      "actual": "O(n × k log k)",
      "distractor": "O(n²)",
      "note": "Sort each of n words of length k as the hash key; map ops are O(1) amortized."
    },
    "p-reverse-list": {
      "actual": "O(n)",
      "distractor": "O(n²)",
      "note": "Single pass swapping next-pointers; each node touched exactly once."
    },
    "p-cycle": {
      "actual": "O(n)",
      "distractor": "O(n²)",
      "note": "Floyd's tortoise/hare — fast pointer catches slow in linear steps."
    },
    "p-merge-two-sorted": {
      "actual": "O(n + m)",
      "distractor": "O(n × m)",
      "note": "Single walk advancing whichever list has the smaller head; each node enqueued once."
    },
    "p-invert": {
      "actual": "O(n)",
      "distractor": "O(log n)",
      "note": "Visits every node once to swap children — O(n), not O(log n) (height-only bounds don't apply when you touch every node)."
    },
    "p-max-depth": {
      "actual": "O(n)",
      "distractor": "O(log n)",
      "note": "Recursion visits every node to compute the depth — linear, not log (log n applies only to balanced search, not full traversal)."
    },
    "p-same-tree": {
      "actual": "O(n)",
      "distractor": "O(n log n)",
      "note": "Pairwise pre-order traversal; each pair of nodes compared once."
    },
    "p-islands": {
      "actual": "O(rows × cols)",
      "distractor": "O((rows × cols)²)",
      "note": "Each cell DFS-visited at most once across all island-starts (sink-in-place via the visited mark)."
    },
    "p-num-provinces": {
      "actual": "O(n²)",
      "distractor": "O(n)",
      "note": "Union-find over the n × n adjacency matrix — every cell visited; near-linear in n² with path compression."
    },
    "p-permutations": {
      "actual": "O(n × n!)",
      "distractor": "O(2ⁿ)",
      "note": "n! permutations × O(n) work per permutation to copy/track choices — not 2ⁿ (that's subsets)."
    },
    "p-subsets": {
      "actual": "O(n × 2ⁿ)",
      "distractor": "O(n!)",
      "note": "2ⁿ subsets × O(n) work per subset to copy — not n! (that's permutations)."
    },
    "p-trapping-rain": {
      "actual": "O(n)",
      "distractor": "O(n²)",
      "note": "Two-pointer pass tracking max-left/max-right walls — single linear sweep, no nested loop."
    },
    "p-coin-change": {
      "actual": "O(amount × coins)",
      "distractor": "O(amount²)",
      "note": "DP table fills in amount rows × coins columns — the coin count is the multiplier, not amount itself."
    },
    "p-top-k-frequent": {
      "actual": "O(n log k)",
      "distractor": "O(n log n)",
      "note": "Min-heap of size k — n inserts each log k, not log n (heap is bounded at k)."
    },
    "p-kth-largest": {
      "actual": "O(n log k)",
      "distractor": "O(n)",
      "note": "Min-heap of size k — log k per insert, not amortized O(1) (heap insert is logarithmic)."
    },
    "p-clone-graph": {
      "actual": "O(V + E)",
      "distractor": "O(V × E)",
      "note": "DFS/BFS visiting every node once and every edge once — graph-traversal standard."
    }
  }
}
