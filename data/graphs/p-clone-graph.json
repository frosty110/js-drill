{
  "id": "p-clone-graph",
  "title": "Clone Graph",
  "section": "Graphs",
  "track": "patterns",
  "status": "full",
  "description": "DFS with a visited Map from original-node to clone-node. The Map handles cycles AND avoids exponential reclones of shared neighbors.",
  "mechanics": ["dp-memoization", "dfs-graph-adjacency"],
  "conversation": {
    "intro": "How an interview about this problem would actually play out. Each section has two parts: what you'd actually say out loud, and why that move matters. Tap a section to expand.",
    "sections": [
      {
        "title": "1. Restate & clarify",
        "prompt": "Before touching code — pin down the inputs.",
        "say": "\"Let me make sure I have this right: I'm given a reference to one node in a connected, undirected graph (each node has `val` and `neighbors: Node[]`), and I need to return a deep copy — a brand-new graph with the same structure, where no node in the result is the same object reference as any node in the input.\"\n\nThen a few quick clarifying questions:\n• Connected — am I guaranteed the input graph is connected? (Otherwise I might miss components.)\n• Cycles — the graph can have cycles, right? (Almost always yes; that's why the visited Map exists.)\n• Self-loops — could a node be its own neighbor?\n• Null input — return null?\n• Node values — unique, or could two distinct nodes have the same `val`? (Critical: if vals can collide, my Map must key on the original-node OBJECT, not `node.val`.)",
        "why": "The unique-vs-non-unique values question is the load-bearing one. If you key the visited Map on `node.val` and two different original nodes share a value, you'd alias them in the clone — wrong graph. The correct key is the original-node object reference itself (JavaScript `Map` supports object keys with identity comparison). The cycles question forces the visited-Map insight; if the interviewer says 'tree only', you don't strictly need the map (but it's still fine to use one). Asking signals you've thought about what makes the problem hard."
      },
      {
        "title": "2. Brute force first (out loud)",
        "prompt": "Name the broken approach to motivate the fix.",
        "say": "\"The naive approach is: walk the graph and for each neighbor, recursively call `clone(neighbor)`. The problem is cycles — if node A has neighbor B and B has neighbor A, you'd recurse from A into B, then from B back into A, forever. Even without cycles, if multiple nodes share a neighbor (say a diamond graph A→B, A→C, B→D, C→D), you'd clone D twice — once via B's recursion and once via C's — producing two D-copies that the cloned graph treats as different nodes. Wrong structure AND wasted work.\"",
        "why": "Two reasons to name the broken version. (1) It motivates the visited Map: 'I need to remember which originals I've already cloned, both to break cycles and to share clones across paths.' (2) It distinguishes this problem from the tree-copy version (where there are no cycles and no shared nodes, so naive recursion works). The diamond example is concrete and shows that the bug isn't only about cycles — sharing alone breaks the clone."
      },
      {
        "title": "3. Spot the pattern",
        "prompt": "What signal in the problem points to DFS with a visited Map?",
        "say": "\"Two things tell me 'DFS with memoization on a graph':\n1. I need to visit every node and rewrite the edges — that's DFS/BFS over a connected graph.\n2. I need to handle cycles and shared neighbors — that's what the visited set is for.\nThe twist for this problem is that the visited structure is a Map, not a Set: it stores `original → clone`, so it doubles as the 'have I cloned this?' check AND the source of the cached clone object. The load-bearing rule is: insert `map.set(orig, copy)` BEFORE recursing into the neighbors. If you recurse first, the cycle re-enters DFS on the same original, finds nothing in the map, makes a SECOND copy, recurses again, and infinite-loops. Set first, then recurse — that's the whole algorithm in one sentence.\"",
        "why": "Naming the pattern AND the specific implementation rule (set-before-recurse) is what makes this conversation feel earned. 'Use a visited Map' is the textbook tip; 'set before recurse, because a cycle re-enters DFS during the neighbor loop' is the derivation that proves you've actually written this and bug-fixed it. Interviewers will pull on that thread — the wrong order is the most common bug they see, and being able to explain WHY it's wrong (not just 'I memorized to do it this way') separates senior from junior."
      },
      {
        "title": "4. Trace through examples",
        "prompt": "Walk a few inputs to confirm the model before writing the loop.",
        "intro": "\"Let me trace through a couple of cases to make sure my model is right.\" Three inputs cover the interesting behaviors — a 4-cycle with cross-edges (the load-bearing cycle case), a single node with no edges (the base case), and a triangle (the simplest cycle that forces the visited Map).",
        "examples": [
          {
            "input": "adj=[[1,3],[0,2],[1,3],[0,2]]  (4-cycle 1↔2↔3↔4↔1, no diagonals)",
            "output": "cloned graph with same adjacency structure",
            "note": "Classic case — cycles in all directions. Watch the Map record each clone BEFORE recursing.",
            "trace": "dfs(orig=1): map.has(1)? no → create copy(1), map.set(1, copy1)\n  for nb in [2, 4]:\n    dfs(orig=2): map.has(2)? no → create copy(2), map.set(2, copy2)\n      for nb in [1, 3]:\n        dfs(orig=1): map.has(1)? YES → return copy1 (cycle broken!)\n        copy2.neighbors.push(copy1)\n        dfs(orig=3): map.has(3)? no → create copy(3), map.set(3, copy3)\n          for nb in [2, 4]:\n            dfs(orig=2): map.has(2)? YES → return copy2 (cycle broken!)\n            copy3.neighbors.push(copy2)\n            dfs(orig=4): map.has(4)? no → create copy(4), map.set(4, copy4)\n              for nb in [1, 3]: both YES → return copy1, copy3\n            copy3.neighbors.push(copy4)\n        copy2.neighbors.push(copy3)\n    copy1.neighbors.push(copy2)\n    dfs(orig=4): map.has(4)? YES → return copy4 (already cloned via copy3's path)\n  copy1.neighbors.push(copy4)\nfinal map: {1→copy1, 2→copy2, 3→copy3, 4→copy4} — 4 clones for 4 originals ✓"
          },
          {
            "input": "adj=[[]]  (single node, no neighbors)",
            "output": "single cloned node with empty neighbors",
            "note": "Base case — no cycles to handle, just verify the function doesn't choke on empty neighbors.",
            "trace": "dfs(orig=1): map.has(1)? no → create copy(1) with neighbors=[]\nmap.set(1, copy1)\nfor nb in []: loop doesn't execute\nreturn copy1"
          },
          {
            "input": "adj=[[1,2],[0,2],[0,1]]  (triangle 1-2-3-1)",
            "output": "cloned triangle",
            "note": "Smallest cycle that exercises both branches of map.has — verify clones are reused, not duplicated.",
            "trace": "dfs(orig=1): map.set(1, copy1)\n  dfs(orig=2): map.set(2, copy2)\n    dfs(orig=1): map.has(1)? YES → return copy1\n    copy2.neighbors.push(copy1)\n    dfs(orig=3): map.set(3, copy3)\n      dfs(orig=1): YES → return copy1\n      dfs(orig=2): YES → return copy2\n      copy3.neighbors = [copy1, copy2]\n    copy2.neighbors.push(copy3)\n  copy1.neighbors.push(copy2)\n  dfs(orig=3): YES → return copy3\ncopy1.neighbors.push(copy3)\nfinal: copy1.neighbors=[copy2, copy3], copy2=[copy1, copy3], copy3=[copy1, copy2]"
          }
        ],
        "why": "Tracing surfaces the set-before-recurse rule viscerally. Look at the very first cycle in example 1: `dfs(1)` calls `dfs(2)` which calls `dfs(1)` again — and the only reason this terminates is that `dfs(1)` already wrote `map.set(1, copy1)` before entering its neighbor loop. If you'd written `map.set(orig, copy)` AFTER the for-loop instead of before, the inner `map.has(1)` would return false, you'd create a SECOND copy of 1, recurse into ITS neighbors, and infinite-loop. The trace makes the bug-and-fix concrete in a way that 'use a visited Map' never does."
      },
      {
        "title": "5. Edge cases to mention",
        "prompt": "What inputs would trip up a careless implementation?",
        "say": "\"Before I declare it done, let me think about edges:\n• Null input → return null at the top. Guard the function entry.\n• Single node, no neighbors → DFS creates one copy, neighbor loop never executes, returns copy. ✓\n• Self-loop (node A's neighbors include A itself) → first thing DFS does is set `map.set(A, copyA)`, then loop into neighbors. When it hits A as its own neighbor, `map.has(A)` is true → returns copyA, pushes copyA into copyA's neighbors. Self-loop preserved in the clone. ✓\n• Disconnected graph — problem statement usually says 'connected', but if not, this DFS only clones the connected component of the input node. The interviewer would have to give us roots for each component or a list of all nodes.\n• Very large graph — recursion depth = longest DFS path = up to V. Could stack-overflow at ~10k nodes in JS. The iterative BFS variant uses an explicit queue and avoids the stack.\n• Duplicate values across distinct nodes (e.g. two different nodes both with val=1) — the Map keys on the original node OBJECT, not its val, so they stay distinct. This is why I asked about val-uniqueness up front.\"",
        "why": "The self-loop case is the cheap proof that 'set before recurse' is the right rule even outside cycles — it works the same way. The duplicate-values case is the senior insight that explains WHY the Map key must be the node object, not the val; most candidates use the val as the key without realizing it's wrong when vals collide. Calling that out shows you've thought about the data model, not just the algorithm. The recursion-depth warning is the same concern as Number of Islands — JS stacks aren't deep enough for graph-scale recursion."
      },
      {
        "title": "6. Complexity & wrap-up",
        "prompt": "How would you defend the complexity claim?",
        "say": "\"Time is O(V + E) — every node is created exactly once (V), and every edge is traversed exactly twice in undirected form (once from each endpoint), so the neighbor pushes sum to 2E. The Map operations (has, get, set) are O(1) amortized for object keys.\n\nSpace is O(V) for the Map plus O(V) worst case for the recursion stack on a path-shaped graph, so O(V) overall — independent of E, which is nice.\n\nIf you wanted to extend to BFS instead of DFS, the algorithm is structurally the same: queue the original, create its copy and `map.set`, then for each dequeued original, look at its neighbors — if not in the map, create the copy and enqueue; either way wire `copy.neighbors.push(map.get(nb))`. Same Map, same set-before-recurse discipline, just iteration instead of recursion. That's what you'd reach for if recursion-depth is a concern.\n\nThe same template — 'visited Map from original-thing → clone-thing, set before recurse' — shows up in Copy List with Random Pointer (where the Map handles the random pointer just like neighbors handle cycles) and any 'deep clone a graph-like structure with shared references' problem.\"",
        "why": "The 'each edge traversed exactly twice in undirected form' argument is the textbook defense; for directed graphs it's once. The DFS-to-BFS extension is the obvious follow-up and proves you see the algorithm shape (visited-Map + frontier) rather than a specific syntax. The Copy List with Random Pointer connection is the killer cross-reference — same Map trick, different data structure, and naming it explicitly demonstrates pattern-recognition at the template level."
      }
    ]
  },
  "walkthrough": {
    "intro": "Step through DFS clone with a visited Map. The trace builds the original graph from the adjacency list, then walks the recursive clone (using yield* to delegate into nested calls) so you can watch the map fill and the cycle break.",
    "examples": [
      { "label": "4-cycle 1-2-3-4-1 (with diagonals) → cloned",  "input": { "adj": [[1, 3], [0, 2], [1, 3], [0, 2]] }, "expected": "[[2,4],[1,3],[1,3],[2,4]]" },
      { "label": "single node, no edges → cloned",                "input": { "adj": [[]] },                            "expected": "[[]]" },
      { "label": "triangle 1-2-3-1 → cloned",                     "input": { "adj": [[1, 2], [0, 2], [0, 1]] },         "expected": "[[2,3],[1,3],[1,2]]" }
    ],
    "trace": [
      "function* trace(input) {",
      "  const { adj } = input;",
      "  const nodes = adj.map((_, i) => ({ val: i + 1, neighbors: [] }));",
      "  for (let i = 0; i < adj.length; i++) {",
      "    for (const j of adj[i]) nodes[i].neighbors.push(nodes[j]);",
      "  }",
      "  const node = nodes[0];",
      "  yield { line: 1, label: `enter cloneGraph — root.val=${node.val}, adj=${JSON.stringify(adj)}`, state: { adj, rootVal: node.val } };",
      "  if (!node) {",
      "    yield { line: 2, label: 'node is null → return null', state: { returns: 'null' } };",
      "    return;",
      "  }",
      "  const map = new Map();",
      "  yield { line: 4, label: 'init map (empty) — keyed by original-node identity', state: { mapSize: 0 } };",
      "  function* dfs(orig, depth) {",
      "    yield { line: 5, label: `dfs(${orig.val}) — depth=${depth}, mapSize=${map.size}`, state: { current: orig.val, depth, mapSize: map.size, mapped: [...map.keys()].map(k => k.val) } };",
      "    if (map.has(orig)) {",
      "      yield { line: 7, label: `${orig.val} already cloned — return cached copy (breaks cycle)`, state: { current: orig.val, depth, mapped: [...map.keys()].map(k => k.val) } };",
      "      return map.get(orig);",
      "    }",
      "    const copy = { val: orig.val, neighbors: [] };",
      "    yield { line: 8, label: `create copy for ${orig.val} (no neighbors yet)`, state: { current: orig.val, depth, mapped: [...map.keys()].map(k => k.val) } };",
      "    map.set(orig, copy);",
      "    yield { line: 10, label: `map.set(${orig.val}, copy) BEFORE recursing — load-bearing for cycles`, state: { current: orig.val, depth, mapSize: map.size, mapped: [...map.keys()].map(k => k.val) } };",
      "    for (const nb of orig.neighbors) {",
      "      const clonedNb = yield* dfs(nb, depth + 1);",
      "      copy.neighbors.push(clonedNb);",
      "      yield { line: 11, label: `wire copy(${orig.val}) → copy(${nb.val})`, state: { current: orig.val, depth, neighborsSoFar: copy.neighbors.map(n => n.val), mapped: [...map.keys()].map(k => k.val) } };",
      "    }",
      "    yield { line: 12, label: `done with ${orig.val} — return its copy`, state: { current: orig.val, depth, neighbors: copy.neighbors.map(n => n.val) } };",
      "    return copy;",
      "  }",
      "  const cloned = yield* dfs(node, 0);",
      "  const seen = new Set([cloned]);",
      "  const queue = [cloned];",
      "  const flatAdj = [];",
      "  while (queue.length) {",
      "    const n = queue.shift();",
      "    flatAdj.push(n.neighbors.map(x => x.val));",
      "    for (const nb of n.neighbors) if (!seen.has(nb)) { seen.add(nb); queue.push(nb); }",
      "  }",
      "  yield { line: 14, label: `return clone — flattened adj=${JSON.stringify(flatAdj)}`, state: { mapSize: map.size, returns: JSON.stringify(flatAdj) } };",
      "}"
    ]
  },
  "reference": {
    "code": "function cloneGraph(node) {\n  if (!node) return null;\n  // Map original-node OBJECT → clone — identity keys, vals can repeat.\n  const map = new Map();\n  function dfs(orig) {\n    // Already cloned (or mid-clone) — return the recorded copy to break cycles.\n    if (map.has(orig)) return map.get(orig);\n    const copy = { val: orig.val, neighbors: [] };\n    // CRITICAL: record BEFORE recursing — otherwise a cycle infinite-loops.\n    map.set(orig, copy);\n    for (const nb of orig.neighbors) copy.neighbors.push(dfs(nb));\n    return copy;\n  }\n  return dfs(node);\n}\n// Time O(V + E), Space O(V) for the map + recursion stack",
    "notes": [
      "CRITICAL: set map.set(orig, copy) BEFORE recursing into neighbors. Otherwise a cycle re-enters dfs and infinite-loops.",
      "The Map keys are the original node OBJECTS (identity), not their .val — vals can repeat.",
      "For each original node you create exactly one clone, then wire its neighbors by recursing or fetching from the map."
    ]
  },
  "L1": {
    "questions": [
      {
        "q": "Why must you put the clone in the map BEFORE recursing into neighbors?",
        "options": [
          "So the neighbor recursion can mutate the parent's copy via the shared Map reference",
          "Otherwise a cycle re-enters dfs on a node that is not yet recorded and recurses forever",
          "Map.set assigns a stable identity hash that neighbor recursions rely on for ordering",
          "The Map's insertion order determines the clone's neighbor order in the result"
        ],
        "answer": 1
      },
      {
        "q": "What are the keys of the visited Map?",
        "options": [
          "Node values (.val)",
          "Original node object references",
          "Clone references",
          "A tuple of (original, clone) pairs, with the original side compared by identity"
        ],
        "answer": 1
      },
      {
        "q": "Time complexity?",
        "options": [
          "O(V)",
          "O(V + E)",
          "O(V * E)",
          "O(V^2)"
        ],
        "answer": 1
      },
      {
        "q": "For a null input, the function returns:",
        "options": [
          "An empty node `{ val: null, neighbors: [] }` to preserve the deep-copy contract",
          "null",
          "undefined",
          "throws"
        ],
        "answer": 1,
        "explain": "The guard `if (!node) return null` handles the empty-graph case before any cloning begins."
      },
      {
        "q": "Why is a plain visited SET insufficient here, requiring a MAP instead?",
        "options": [
          "Set.has takes O(n) on objects while Map.has is O(1), so cycles would be too slow with a Set",
          "You need the original->clone mapping to wire each clone's neighbors to the right clone objects",
          "Set iteration order isn't insertion-preserving, which would scramble the neighbor order in the clones",
          "A Set can only break cycles for directed graphs; undirected graphs need a Map's bidirectional lookup"
        ],
        "answer": 1,
        "explain": "A Set only answers 'seen this?'; cloning needs to fetch the EXISTING clone for an already-seen original so neighbors point at shared clones, which requires the value side of a Map."
      },
      {
        "q": "Besides breaking cycles, what second problem does the visited Map solve?",
        "options": [
          "It guarantees the clone's neighbor lists are in the same insertion order as the original",
          "It ensures a neighbor shared by multiple nodes (e.g. a diamond) is cloned exactly once",
          "It records the depth of each node so the recursion knows when to terminate",
          "It distinguishes nodes with equal .val by appending a unique tag to each clone"
        ],
        "answer": 1,
        "explain": "Without the Map, a node reachable via two paths (diamond A->B->D, A->C->D) would be cloned twice, producing two distinct D-copies and a wrong structure."
      },
      {
        "q": "Could you solve Clone Graph with BFS instead of DFS?",
        "options": [
          "No — BFS would clone nodes level-by-level, but cycles span levels and would be cloned twice",
          "Yes — use a queue plus the same original->clone Map; both traverse every node once",
          "Yes, but the clones must be wired up in a second pass after BFS finishes enumerating all originals",
          "No — BFS visits each node before its neighbors are cloned, so neighbor pointers would be null"
        ],
        "answer": 1,
        "explain": "BFS with an explicit queue and the identical visited Map clones correctly and avoids deep recursion; the Map (set-before-enqueue) is what handles cycles in either traversal."
      },
      {
        "q": "Why MUST the Map key be the original node OBJECT, not node.val?",
        "options": [
          "Map.get(val) does linear scan on numeric keys; object keys hash in O(1)",
          "If two distinct nodes share a val, keying on val would alias them into one clone — wrong graph",
          "Map keys must be reference types — primitives are silently converted to Object wrappers that lose equality",
          "node.val could be mutated during traversal, invalidating prior Map entries"
        ],
        "answer": 1,
        "explain": "Map uses identity for object keys, so distinct originals stay distinct even with equal vals; keying on val collapses different nodes when values repeat."
      }
    ]
  },
  "L2": {
    "exercises": [
      {
        "prompt": "Fill in the DFS clone. The order of the two lines after creating copy is load-bearing.",
        "template": "function cloneGraph(node) {\n  if (!node) return null;\n  const map = new ___();\n  function dfs(orig) {\n    if (map.has(orig)) return map.___(orig);\n    const copy = { val: orig.val, neighbors: [] };\n    map.set(orig, ___);\n    for (const nb of orig.neighbors) copy.neighbors.push(___(nb));\n    return copy;\n  }\n  return dfs(node);\n}\nconst a = { val: 1, neighbors: [] };\nconst b = { val: 2, neighbors: [] };\nconst c = { val: 3, neighbors: [] };\na.neighbors = [b, c];\nb.neighbors = [a, c];\nc.neighbors = [a, b];\nconst cloned = cloneGraph(a);\nconsole.log(cloned !== a);\nconsole.log(cloned.val + \":\" + cloned.neighbors.map(n => n.val).join(\",\"));",
        "blanks": [
          {
            "answer": "Map",
            "hint": "identity-keyed lookup"
          },
          {
            "answer": "get",
            "hint": "already cloned — return the stored copy"
          },
          {
            "answer": "copy",
            "hint": "record the clone BEFORE recursing"
          },
          {
            "answer": "dfs",
            "hint": "recurse to clone the neighbor"
          }
        ],
        "expectedOutput": "true\n1:2,3"
      }
    ]
  },
  "L3": {
    "prompt": "Write `cloneGraph(node)` using DFS + a visited Map. Build the 4-cycle graph with edges 1-2, 2-3, 3-4, 4-1, clone it, then log (1) `cloned !== original` (deep-copy proof), (2) a DFS-order serialization as `val:[n1,n2,...]` entries space-separated.",
    "expectedOutput": "true\n1:[2,4] 2:[1,3] 3:[2,4] 4:[1,3]",
    "canonical": "function cloneGraph(node) {\n  if (!node) return null;\n  // Map original-node OBJECT → clone — identity keys, vals can repeat.\n  const map = new Map();\n  function dfs(orig) {\n    // Already cloned (or mid-clone) — return the recorded copy to break cycles.\n    if (map.has(orig)) return map.get(orig);\n    const copy = { val: orig.val, neighbors: [] };\n    // CRITICAL: record BEFORE recursing — otherwise a cycle infinite-loops.\n    map.set(orig, copy);\n    for (const nb of orig.neighbors) copy.neighbors.push(dfs(nb));\n    return copy;\n  }\n  return dfs(node);\n}\nfunction serialize(start) {\n  const seen = new Set();\n  const order = [];\n  function walk(n) {\n    if (seen.has(n)) return;\n    seen.add(n);\n    order.push(n);\n    for (const nb of n.neighbors) walk(nb);\n  }\n  walk(start);\n  return order.map(n => n.val + \":[\" + n.neighbors.map(x => x.val).join(\",\") + \"]\").join(\" \");\n}\nconst a = { val: 1, neighbors: [] };\nconst b = { val: 2, neighbors: [] };\nconst c = { val: 3, neighbors: [] };\nconst d = { val: 4, neighbors: [] };\na.neighbors = [b, d];\nb.neighbors = [a, c];\nc.neighbors = [b, d];\nd.neighbors = [a, c];\nconst cloned = cloneGraph(a);\nconsole.log(cloned !== a);\nconsole.log(serialize(cloned));",
    "hints": [
      "Map original-node → clone-node, keyed by object identity.",
      "Record the clone in the map BEFORE recursing into its neighbors, or a cycle infinite-loops.",
      "For each neighbor, push dfs(neighbor) — dfs returns either the cached clone or a fresh one."
    ]
  }
}
