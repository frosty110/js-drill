{
  "id": "p-course",
  "title": "Course Schedule",
  "section": "Graphs",
  "track": "patterns",
  "status": "full",
  "description": "Cycle detection on a directed graph via Kahn topological sort: peel zero-indegree nodes until none remain. If all peeled, no cycle.",
  "mechanics": ["array-as-queue"],
  "conversation": {
    "intro": "How an interview about this problem would actually play out. Each section has two parts: what you'd actually say out loud, and why that move matters. Tap a section to expand.",
    "sections": [
      {
        "title": "1. Restate & clarify",
        "prompt": "Before touching code — pin down the inputs.",
        "say": "\"Let me make sure I have this right: I get a number of courses `numCourses` (numbered 0 to n-1) and a list of `prerequisites` where each pair `[a, b]` means I must take course b before I can take course a. I return true if it's possible to finish all courses, false otherwise.\"\n\nThen a few quick clarifying questions:\n• Edge direction in the pair — `[a, b]` means b is a prereq of a, right? (Easy to flip and get a wrong-direction graph.)\n• Empty prerequisites — `numCourses=3, prereqs=[]` returns true?\n• Duplicate edges — could `[[1,0],[1,0]]` appear? Does it matter? (Inflates indegree but the algorithm still works.)\n• Self-loops — could `[[1,1]]` appear? That's a trivial cycle, returns false.\n• Disconnected components — multiple independent course chains?",
        "why": "The edge-direction question is the most common bug. The convention in this problem is `[a, b]` = 'b before a', so the directed edge goes FROM b TO a (b unlocks a). If you flip it, the algorithm runs but on a wrong graph and gives wrong answers. Asking up front signals you've seen the problem before and forces the interviewer to confirm — much better than discovering it via a failing test. The self-loop and duplicate-edge questions are senior-level; they probe the input contract."
      },
      {
        "title": "2. Brute force first (out loud)",
        "prompt": "Name the obvious approach before optimizing.",
        "say": "\"The brute force is exhaustive search: try every permutation of `numCourses!` orderings, and for each one check whether every prerequisite is satisfied — meaning b comes before a for every `[a, b]`. That's O(n! · m) where m is the number of prereqs, which is hopeless past n=10. The fix is to recognize this is cycle detection in a directed graph: if there's no cycle, a valid order exists; if there is, no permutation can satisfy it.\"",
        "why": "Two reasons to name the n!-permutations brute force. (1) It frames the problem as a search through orderings, which is what it literally is — every permutation either satisfies all constraints or doesn't. (2) It motivates the leap to graph theory: 'I don't actually need to construct the order, I just need to know if one CAN exist, which is a yes/no question about cycles.' That reframing is what unlocks both Kahn's BFS and DFS-with-colors. The brute force isn't a strawman — it's literally what people try first if they don't see the graph framing."
      },
      {
        "title": "3. Spot the pattern",
        "prompt": "What signal in the problem points to topological sort / cycle detection?",
        "say": "\"'Must do X before Y' is the canonical directed-graph trigger. Each course is a node; each `[a, b]` is a directed edge b → a (b unlocks a). The question 'can I finish all courses?' is exactly 'does this directed graph have a cycle?' — because a cycle means some course depends, transitively, on itself, which is impossible to satisfy. I'll use Kahn's algorithm: build adjacency and indegree arrays, seed a queue with every node that has indegree 0 (no prereqs), then BFS — for each node I dequeue I 'take' that course and decrement indegree on all its dependents. Any dependent whose indegree hits 0 joins the queue. At the end, if I took every course (`taken === numCourses`), no cycle; if I came up short, the missing nodes form a cycle. Alternative is DFS with 3-color marking (white/gray/black) — gray means 'on current path', and re-encountering a gray node IS the cycle. Kahn's tends to be cleaner for this problem because the queue gives you the topological order for free (which matters for Course Schedule II) and there's no recursion stack.\"",
        "why": "Naming the pattern AND the structural property AND comparing the two valid implementations is the senior move. 'It's a cycle detection problem' is the trigger; 'Kahn's BFS because the topological ordering is a natural byproduct and there's no stack risk' is the implementation choice with justification. The DFS-with-colors mention shows you know both — interviewers may push 'why not DFS?' and the answer is 'either works; I'm picking Kahn's for these specific reasons.' Don't bury that — it's the kind of taste interviewers explicitly grade."
      },
      {
        "title": "4. Trace through examples",
        "prompt": "Walk a few inputs to confirm the model before writing the loop.",
        "intro": "\"Let me trace through a couple of cases to make sure my model is right.\" Three inputs cover the interesting behaviors — a simple linear DAG (success), a 2-node cycle (failure), and a diamond DAG (multi-source, tests that all zero-indegree nodes seed correctly).",
        "examples": [
          {
            "input": "numCourses=2, prerequisites=[[1,0]]",
            "output": "true",
            "note": "Simplest valid case — take 0, then take 1. indeg[0]=0 seeds the queue.",
            "trace": "build: graph[0]=[1], indeg=[0, 1]\nseed: indeg[0]===0 → queue=[0]\ndequeue 0 → taken=1, decrement indeg[1]: 1→0 → enqueue 1\ndequeue 1 → taken=2, graph[1] empty\nqueue empty → taken(2) === numCourses(2) → return true"
          },
          {
            "input": "numCourses=2, prerequisites=[[1,0],[0,1]]",
            "output": "false",
            "note": "Direct 2-cycle: 0 needs 1, 1 needs 0. Both indegrees start at 1, queue never seeds.",
            "trace": "build: graph[0]=[1], graph[1]=[0], indeg=[1, 1]\nseed loop: indeg[0]=1, indeg[1]=1 → queue stays []\nwhile loop never executes → taken=0\ntaken(0) !== numCourses(2) → return false"
          },
          {
            "input": "numCourses=6, prerequisites=[[1,0],[2,0],[3,1],[3,2],[4,3],[5,4]]",
            "output": "true",
            "note": "Diamond DAG — course 0 unlocks both 1 and 2, which both unlock 3, then linear to 5. Confirms multi-out-edge decrement and that 3 enqueues only after BOTH 1 and 2 finish.",
            "trace": "build: graph[0]=[1,2], graph[1]=[3], graph[2]=[3], graph[3]=[4], graph[4]=[5]\nindeg=[0, 1, 1, 2, 1, 1]\nseed: only indeg[0]=0 → queue=[0]\ndequeue 0 → taken=1, decrement indeg[1]: 1→0 → enqueue 1; indeg[2]: 1→0 → enqueue 2 → queue=[1,2]\ndequeue 1 → taken=2, decrement indeg[3]: 2→1 (NOT 0 yet)\ndequeue 2 → taken=3, decrement indeg[3]: 1→0 → enqueue 3 → queue=[3]\ndequeue 3 → taken=4, decrement indeg[4]: 1→0 → enqueue 4\ndequeue 4 → taken=5, decrement indeg[5]: 1→0 → enqueue 5\ndequeue 5 → taken=6\nqueue empty → taken(6) === numCourses(6) → return true"
          }
        ],
        "why": "Tracing surfaces the two bugs this problem keeps creating. (1) The diamond DAG case confirms that node 3 only enqueues AFTER both 1 and 2 have decremented its indegree — if you instead enqueued on `indeg > 0`, you'd take 3 too early. (2) The 2-cycle case confirms that an empty initial queue is correctly handled (the while loop just doesn't execute), and that the `taken === numCourses` check at the end is what catches the cycle — without that check you'd silently return true on a stuck graph. Worth flagging both before writing the loop."
      },
      {
        "title": "5. Edge cases to mention",
        "prompt": "What inputs would trip up a careless implementation?",
        "say": "\"Before I declare it done, let me think about edges:\n• Empty prerequisites — every indegree is 0, every node seeds the queue, all `taken` in one sweep, returns true. ✓\n• Single course `numCourses=1, prereqs=[]` — indeg=[0], queue=[0], taken=1, returns true. ✓\n• Self-loop `[[1,1]]` — indeg[1]=1 with no path to reach 0; queue empty → false. The algorithm handles it without a special case.\n• Duplicate edges `[[1,0],[1,0]]` — indeg[1]=2; when 0 is taken, it decrements twice (graph[0]=[1,1]), 1 hits 0 correctly. Works but inflates work; could dedup if asked.\n• Disconnected components — multiple zero-indegree roots seed the queue in parallel; BFS processes them all. No special handling.\n• Very large input (1e5 courses, 1e5 edges) — `queue.shift()` is O(n) on a JS array, which makes the loop O(V²). For interview correctness it's fine; for production I'd use a circular buffer or a head index to keep `shift` O(1).\"",
        "why": "The self-loop case is the cheap one most candidates forget to mention — it's a cycle of length 1, and the algorithm handles it for the same structural reason it handles longer cycles (initial indegree > 0, never enqueued). The `queue.shift()` performance call-out is the senior move: it's an actual JS-specific concern that doesn't apply in Python/Java, and most candidates write `.shift()` without thinking. Interviewers either nod (impressed) or say 'assume it's fine' — either way you've shown you read your own code."
      },
      {
        "title": "6. Complexity & wrap-up",
        "prompt": "How would you defend the complexity claim?",
        "say": "\"Time is O(V + E) where V = numCourses and E = prerequisites.length. The build phase is O(V + E). The BFS visits each node at most once (V dequeues) and each edge at most once (one decrement per edge across the whole loop), so the body is O(V + E). The only nuance is `queue.shift()` — that's O(V) per call in JS, which would make the loop O(V²); for big inputs use a head-index pointer or a Deque-equivalent.\n\nSpace is O(V + E) — the adjacency list dominates with O(E), the indegree array and queue are O(V).\n\nIf you wanted to extend to Course Schedule II — return the actual order, not just the boolean — it's literally one extra line: push each `c` you dequeue into an `order` array, and return `order` instead of `taken === numCourses` (returning `[]` on cycle). Kahn's is preferred over DFS specifically because the topological order falls out for free. If you wanted to extend to 'find the minimum number of semesters' (Course Schedule III variant), it's still Kahn's but with a layer counter — increment per BFS level, not per dequeue.\"",
        "why": "The V + E defense is the textbook bound — interviewers may ask 'why not V*E?' and the answer is 'each edge contributes one decrement to the inner for-loop across the entire BFS, never repeated.' The `queue.shift()` performance call-out is what shows you care about constant factors in the language you're actually writing. The Course Schedule II extension is the obvious follow-up and proves you see Kahn's as a template (build adjacency + indegree, BFS the zero-indegree frontier, decrement-and-enqueue) that generalizes — same shape shows up in Alien Dictionary, Sequence Reconstruction, and any 'find an order consistent with constraints' problem."
      }
    ]
  },
  "walkthrough": {
    "intro": "Step through Kahn's BFS one statement at a time. Watch the queue seed with zero-indegree nodes, then peel each one and decrement its dependents until either every node is taken (return true) or a cycle traps the rest (return false).",
    "examples": [
      { "label": "2 courses, [[1,0]] → true",            "input": { "numCourses": 2, "prerequisites": [[1, 0]] },                  "expected": true },
      { "label": "2 courses, [[1,0],[0,1]] cycle → false", "input": { "numCourses": 2, "prerequisites": [[1, 0], [0, 1]] },         "expected": false },
      { "label": "6 courses, diamond DAG → true",        "input": { "numCourses": 6, "prerequisites": [[1, 0], [2, 0], [3, 1], [3, 2], [4, 3], [5, 4]] }, "expected": true }
    ],
    "trace": [
      "function* trace(input) {",
      "  const { numCourses, prerequisites } = input;",
      "  yield { line: 1, label: `enter canFinish — numCourses=${numCourses}, prerequisites=${JSON.stringify(prerequisites)}`, state: { numCourses, prerequisites } };",
      "  const graph = Array.from({ length: numCourses }, () => []);",
      "  const indeg = new Array(numCourses).fill(0);",
      "  yield { line: 3, label: 'init empty adjacency + indeg=[0,...,0]', state: { graph: graph.map(g => [...g]), indeg: [...indeg] } };",
      "  for (const [a, b] of prerequisites) {",
      "    graph[b].push(a);",
      "    indeg[a]++;",
      "    yield { line: 6, label: `edge ${b} -> ${a} (take ${b} before ${a}); indeg[${a}]=${indeg[a]}`, state: { graph: graph.map(g => [...g]), indeg: [...indeg] } };",
      "  }",
      "  const queue = [];",
      "  for (let i = 0; i < numCourses; i++) {",
      "    if (indeg[i] === 0) {",
      "      queue.push(i);",
      "      yield { line: 12, label: `seed queue with ${i} (indeg=0)`, state: { graph: graph.map(g => [...g]), indeg: [...indeg], queue: [...queue] } };",
      "    }",
      "  }",
      "  yield { line: 14, label: `seeding done — queue=[${queue.join(',')}]`, state: { graph: graph.map(g => [...g]), indeg: [...indeg], queue: [...queue] } };",
      "  let taken = 0;",
      "  while (queue.length) {",
      "    const c = queue.shift();",
      "    taken++;",
      "    yield { line: 16, label: `dequeue ${c} — taken → ${taken}`, state: { graph: graph.map(g => [...g]), indeg: [...indeg], queue: [...queue], taken, current: c } };",
      "    for (const n of graph[c]) {",
      "      indeg[n]--;",
      "      yield { line: 20, label: `satisfy prereq for ${n}: indeg[${n}] → ${indeg[n]}`, state: { graph: graph.map(g => [...g]), indeg: [...indeg], queue: [...queue], taken, current: c, neighbor: n } };",
      "      if (indeg[n] === 0) {",
      "        queue.push(n);",
      "        yield { line: 21, label: `${n} now has 0 prereqs — enqueue`, state: { graph: graph.map(g => [...g]), indeg: [...indeg], queue: [...queue], taken } };",
      "      }",
      "    }",
      "  }",
      "  const ok = taken === numCourses;",
      "  yield { line: 26, label: `queue empty — taken=${taken}, numCourses=${numCourses} → return ${ok}`, state: { indeg: [...indeg], taken, numCourses, returns: ok } };",
      "}"
    ]
  },
  "reference": {
    "code": "function canFinish(numCourses, prerequisites) {\n  const graph = Array.from({ length: numCourses }, () => []);\n  const indeg = new Array(numCourses).fill(0);\n  // [a, b] means take b before a → edge b -> a, indeg[a]++.\n  for (const [a, b] of prerequisites) {\n    graph[b].push(a); // edge b -> a (take b before a)\n    indeg[a]++;\n  }\n  const queue = [];\n  // Seed with every zero-indegree node — these have no prerequisites.\n  for (let i = 0; i < numCourses; i++) {\n    if (indeg[i] === 0) queue.push(i);\n  }\n  let taken = 0;\n  while (queue.length) {\n    const c = queue.shift();\n    taken++;\n    for (const n of graph[c]) {\n      // Satisfying c removes one prereq from n; queue once it hits 0.\n      indeg[n]--;\n      if (indeg[n] === 0) queue.push(n);\n    }\n  }\n  // Peeled every node iff acyclic; remainder, if any, lives inside a cycle.\n  return taken === numCourses;\n}\n// Time O(V + E), Space O(V + E)",
    "notes": [
      "Kahn BFS produces a topological order as a side effect and uses no recursion stack.",
      "Pair [a, b] means \"take b before a\" — edge b -> a in the prerequisite-to-course direction.",
      "If `taken < numCourses` at the end, the remainder contains a cycle."
    ]
  },
  "L1": {
    "questions": [
      {
        "q": "Course Schedule reduces to which classic graph question?",
        "options": [
          "Shortest path",
          "Minimum spanning tree",
          "Cycle detection in a directed graph",
          "Connected components"
        ],
        "answer": 2
      },
      {
        "q": "In Kahn algorithm, which nodes go into the queue initially?",
        "options": [
          "Nodes with zero out-degree (final courses, nothing depends on them)",
          "Nodes with the highest indegree",
          "Nodes with zero indegree (no remaining prerequisites)",
          "Nodes whose index equals 0, then their dependents in order"
        ],
        "answer": 2
      },
      {
        "q": "Time complexity?",
        "options": [
          "O(V)",
          "O(E)",
          "O(V + E)",
          "O(V * E)"
        ],
        "answer": 2
      },
      {
        "q": "How do you conclude that a cycle exists?",
        "options": [
          "The queue runs out before all nodes are taken",
          "An indegree goes negative",
          "The graph has more edges than nodes",
          "A node appears in the queue twice"
        ],
        "answer": 0,
        "explain": "Kahn peels every zero-indegree node; if `taken < numCourses` at the end, the unpeeled remainder is trapped in a cycle."
      },
      {
        "q": "Why is Union-Find NOT a correct substitute for topological sort here?",
        "options": [
          "Union-Find produces a valid topological order but only when there are no shared prerequisites",
          "Union-Find tracks undirected connectivity and cannot detect a DIRECTED cycle or produce an ordering",
          "Union-Find detects cycles correctly but cannot count them, which Course Schedule requires",
          "Union-Find requires sorted input edges, which prerequisites generally are not"
        ],
        "answer": 1,
        "explain": "Course Schedule is a directed-cycle question; Union-Find only answers 'are these connected?' and would merge a->b and b->a without ever noticing the directed cycle."
      },
      {
        "q": "A student says 'use Dijkstra to find the order.' Why is that the wrong tool?",
        "options": [
          "Dijkstra runs in O(E log V) here, which is asymptotically worse than Kahn's O(V + E)",
          "Dijkstra finds shortest weighted paths; this is an unweighted ordering/cycle-detection problem, not a distance problem",
          "Dijkstra rejects graphs containing cycles, so it would crash on cyclic inputs instead of returning false",
          "Dijkstra explores in BFS order from a single source; with multiple zero-indegree starts it would miss components"
        ],
        "answer": 1,
        "explain": "There are no edge weights or 'shortest' anything here — the question is whether a valid order exists, which is topological sort / cycle detection."
      },
      {
        "q": "The DFS alternative to Kahn detects a cycle by:",
        "options": [
          "Returning to the starting node after visiting every reachable neighbor (a closed traversal walk)",
          "Re-entering a node still marked 'in-progress' (gray) on the current recursion path",
          "Discovering that the recursion's post-order index differs from its pre-order index by more than the subtree size",
          "Finding two distinct DFS trees whose roots both reach the same node"
        ],
        "answer": 1,
        "explain": "Three-color DFS marks a node gray while its subtree is being explored; encountering a gray node again means a back-edge, i.e. a directed cycle."
      },
      {
        "q": "One concrete advantage of Kahn's BFS over the DFS-colors approach for Course Schedule II:",
        "options": [
          "Kahn handles disconnected graphs natively; DFS-colors requires a separate outer loop to launch from each component",
          "The dequeue order IS a valid topological ordering, so you get the course order for free",
          "DFS-colors produces a REVERSE topological order, which would require an extra pass to flip",
          "Kahn correctly handles self-loops; DFS-colors marks the gray node and reports a cycle prematurely"
        ],
        "answer": 1,
        "explain": "Pushing each dequeued node into a result list yields the topological order directly — useful when the problem wants the actual ordering, not just a boolean."
      }
    ]
  },
  "L2": {
    "exercises": [
      {
        "prompt": "Fill in Kahn BFS topological sort.",
        "template": "function canFinish(numCourses, prerequisites) {\n  const graph = Array.from({ length: numCourses }, () => []);\n  const indeg = new Array(numCourses).fill(0);\n  for (const [a, b] of prerequisites) {\n    graph[b].push(a);\n    indeg[a]___;\n  }\n  const queue = [];\n  for (let i = 0; i < numCourses; i++) {\n    if (indeg[i] === ___) queue.push(i);\n  }\n  let taken = 0;\n  while (queue.length) {\n    const c = queue.___();\n    taken++;\n    for (const n of graph[c]) {\n      indeg[n]___;\n      if (indeg[n] === 0) queue.push(n);\n    }\n  }\n  return taken ___ numCourses;\n}\nconsole.log(canFinish(2, [[1, 0]]));",
        "blanks": [
          {
            "answer": "++",
            "hint": "every edge into a bumps its indegree"
          },
          {
            "answer": "0",
            "hint": "seed the queue with nodes having no prerequisites"
          },
          {
            "answer": "shift",
            "hint": "FIFO: take from the front"
          },
          {
            "answer": "--",
            "hint": "one prerequisite of n is now satisfied"
          },
          {
            "answer": "===",
            "hint": "strict equality — peeled every node iff acyclic"
          }
        ],
        "expectedOutput": "true"
      }
    ]
  },
  "L3": {
    "prompt": "Write `canFinish(numCourses, prerequisites)` returning true iff all courses can be taken. Pair `[a, b]` means take b before a. Log `canFinish(2, [[1, 0]])` then `canFinish(2, [[1, 0], [0, 1]])`.",
    "expectedOutput": "true\nfalse",
    "canonical": "function canFinish(numCourses, prerequisites) {\n  const graph = Array.from({ length: numCourses }, () => []);\n  const indeg = new Array(numCourses).fill(0);\n  // [a, b] means take b before a → edge b -> a, indeg[a]++.\n  for (const [a, b] of prerequisites) {\n    graph[b].push(a);\n    indeg[a]++;\n  }\n  const queue = [];\n  // Seed with every zero-indegree node — these have no prerequisites.\n  for (let i = 0; i < numCourses; i++) {\n    if (indeg[i] === 0) queue.push(i);\n  }\n  let taken = 0;\n  while (queue.length) {\n    const c = queue.shift();\n    taken++;\n    for (const n of graph[c]) {\n      // Satisfying c removes one prereq from n; queue once it hits 0.\n      indeg[n]--;\n      if (indeg[n] === 0) queue.push(n);\n    }\n  }\n  // Peeled every node iff acyclic; remainder, if any, lives inside a cycle.\n  return taken === numCourses;\n}\nconsole.log(canFinish(2, [[1, 0]]));\nconsole.log(canFinish(2, [[1, 0], [0, 1]]));",
    "hints": [
      "Build adjacency + indegree arrays from pairs.",
      "Kahn BFS: queue zero-indegree, decrement on dequeue, count peeled."
    ]
  }
}
