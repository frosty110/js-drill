{
  "id": "p-num-provinces",
  "title": "Number of Provinces (Union-Find)",
  "section": "Graphs",
  "track": "patterns",
  "status": "full",
  "description": "Count connected components in an n x n adjacency matrix using Union-Find with path compression. Each union of two distinct roots merges two components — start with n, subtract one per successful union.",
  "mechanics": ["union-find"],
  "reference": {
    "code": "function findCircleNum(isConnected) {\n  const n = isConnected.length;\n  const parent = Array.from({ length: n }, (_, i) => i);\n  function find(x) {\n    while (parent[x] !== x) {\n      parent[x] = parent[parent[x]]; // path compression (halving)\n      x = parent[x];\n    }\n    return x;\n  }\n  let count = n;\n  for (let i = 0; i < n; i++) {\n    for (let j = i + 1; j < n; j++) {\n      if (isConnected[i][j] === 1) {\n        const ra = find(i), rb = find(j);\n        if (ra !== rb) {\n          parent[ra] = rb;\n          count--;\n        }\n      }\n    }\n  }\n  return count;\n}\n// Time ~O(n^2 * α(n)), Space O(n)",
    "notes": [
      "The matrix is symmetric — only iterate j > i to avoid double-unions.",
      "Decrement count only on a SUCCESSFUL union (different roots). Same root → already in one province.",
      "Path compression (halving or full) keeps amortized union/find at near O(α(n)) — effectively constant."
    ]
  },
  "L1": {
    "questions": [
      {
        "q": "Why iterate j from i + 1 instead of 0?",
        "options": [
          "Skip the diagonal only",
          "The matrix is symmetric; (i,j) and (j,i) are the same edge",
          "Performance hack with no correctness impact",
          "Required by JavaScript"
        ],
        "answer": 1
      },
      {
        "q": "When do you decrement the province count?",
        "options": [
          "On every isConnected[i][j] === 1",
          "Only when find(i) and find(j) return different roots",
          "Once per row",
          "After both loops finish"
        ],
        "answer": 1
      },
      {
        "q": "What does path compression do?",
        "options": [
          "Sorts the parent array",
          "Flattens the tree so future finds run faster",
          "Reduces memory by half",
          "Detects cycles"
        ],
        "answer": 1
      },
      {
        "q": "For the matrix [[1,1,0],[1,1,0],[0,0,1]] the answer is:",
        "options": [
          "1",
          "2",
          "3",
          "0"
        ],
        "answer": 1,
        "explain": "Rows 0 and 1 are connected; row 2 is alone — two provinces."
      }
    ]
  },
  "L2": {
    "exercises": [
      {
        "prompt": "Fill in Union-Find for Number of Provinces.",
        "template": "function findCircleNum(isConnected) {\n  const n = isConnected.length;\n  const parent = Array.from({ length: n }, (_, i) => i);\n  function find(x) {\n    while (parent[x] !== x) {\n      parent[x] = parent[parent[x]];\n      x = parent[x];\n    }\n    return x;\n  }\n  let count = ___;\n  for (let i = 0; i < n; i++) {\n    for (let j = i ___; j < n; j++) {\n      if (isConnected[i][j] === 1) {\n        const ra = find(i), rb = find(j);\n        if (ra ___ rb) {\n          parent[ra] = rb;\n          count___;\n        }\n      }\n    }\n  }\n  return count;\n}\nconsole.log(findCircleNum([[1, 1, 0], [1, 1, 0], [0, 0, 1]]));",
        "blanks": [
          {
            "answer": "n",
            "hint": "start with every city as its own province"
          },
          {
            "answer": "+ 1",
            "hint": "skip the symmetric half"
          },
          {
            "answer": "!==",
            "hint": "merge only when roots differ"
          },
          {
            "answer": "--",
            "hint": "successful union drops the count by one"
          }
        ],
        "expectedOutput": "2"
      }
    ]
  },
  "L3": {
    "prompt": "Write `findCircleNum(isConnected)` returning the number of provinces using Union-Find with path compression. Log results for [[1,1,0],[1,1,0],[0,0,1]] and [[1,0,0],[0,1,0],[0,0,1]].",
    "expectedOutput": "2\n3",
    "canonical": "function findCircleNum(isConnected) {\n  const n = isConnected.length;\n  const parent = Array.from({ length: n }, (_, i) => i);\n  function find(x) {\n    while (parent[x] !== x) {\n      parent[x] = parent[parent[x]];\n      x = parent[x];\n    }\n    return x;\n  }\n  let count = n;\n  for (let i = 0; i < n; i++) {\n    for (let j = i + 1; j < n; j++) {\n      if (isConnected[i][j] === 1) {\n        const ra = find(i), rb = find(j);\n        if (ra !== rb) {\n          parent[ra] = rb;\n          count--;\n        }\n      }\n    }\n  }\n  return count;\n}\nconsole.log(findCircleNum([[1, 1, 0], [1, 1, 0], [0, 0, 1]]));\nconsole.log(findCircleNum([[1, 0, 0], [0, 1, 0], [0, 0, 1]]));",
    "hints": [
      "Init parent[i] = i; start count at n.",
      "Iterate upper triangle (j > i) since the matrix is symmetric.",
      "On isConnected[i][j] === 1 union them; decrement count only when the roots actually differ."
    ]
  }
}
