{
  "id": "p-top-k-frequent",
  "title": "Top K Frequent",
  "section": "Heap",
  "track": "patterns",
  "status": "full",
  "description": "Count frequencies in a Map, then keep the K most frequent. Sort + slice is the readable baseline; a size-K min-heap is the O(n log k) upgrade.",
  "mechanics": ["freq-map", "array-chain"],
  "conversation": {
    "intro": "How an interview about this problem would actually play out. Each section has two parts: what you'd actually say out loud, and why that move matters. Tap a section to expand.",
    "sections": [
      {
        "title": "1. Restate & clarify",
        "prompt": "Before touching code — pin down the inputs.",
        "say": "\"Let me restate: given `nums` and integer `k`, return the k most frequent *values* — not the indices, the actual values. Quick checks:\"\n\n• Is `k` guaranteed in range — `1 <= k <= number_of_distinct_values`?\n• Does the output order matter? Most contracts say 'any order'; I'll confirm.\n• Ties — if two values have the same frequency, is there a tiebreaker (smaller value first? insertion order?), or can I pick either?\n• How big is `n`, and how many distinct values relative to `n`? — this affects which optimization wins.",
        "why": "The tie-breaking question is the one most candidates skip and most interviewers care about — if the contract is 'any order,' your sort-by-count is correct; if the contract is 'ties broken by value,' you need a two-key comparator. The 'distinct values vs n' question matters because bucket sort wins when frequencies are dense (frequencies bounded by n), but pays a memory tax when the distinct set is sparse — knowing the shape of the data picks the optimal."
      },
      {
        "title": "2. Brute force first (out loud)",
        "prompt": "Name the obvious approaches before reaching for the heap.",
        "say": "\"The dumbest version: for each distinct value, scan the whole array to count it — O(n²) for the counts alone, then sort the distinct values by count. Obviously bad; we'd never ship this.\n\n\"The straightforward version: one O(n) pass to build a frequency Map (value → count), then sort the entries by count descending and slice the first k. That's O(n) for counting plus O(d log d) for sorting d distinct entries — total O(n log n) in the worst case where every value is distinct. That's the readable baseline and it's what I'd write first.\n\n\"The catch: same as kth-largest — we're sorting all d distinct values when we only need partial order over the top k. If d is huge and k is small, we're wasting work.\"",
        "why": "The double-pass-count version (O(n²)) is worth mentioning briefly to show you wouldn't do it — interviewers occasionally test whether you reach for nested loops out of habit. The sort-and-slice version is the *honest* baseline: it's the answer most engineers would actually ship in production code, and it's a fine answer for an interview. Setting it up as 'readable baseline, here's the optimization' makes the heap upgrade feel like a deliberate refinement rather than a flex."
      },
      {
        "title": "3. Spot the pattern",
        "prompt": "Why heap (and why bucket sort might beat it)?",
        "say": "\"Two viable optimizations once we have the frequency Map. First, a *size-k min-heap keyed by frequency* — same logic as kth-largest: walk the d entries, push each, pop when the heap exceeds size k. After the pass, the heap holds the k entries with the highest frequencies. Time becomes O(d log k), space O(k). When k << d, this is a real win.\n\n\"Second, *bucket sort by frequency* — and this is the slicker trick for this specific problem. Frequencies are bounded: no value can appear more than n times. So we can build an array `buckets` of length n+1 where `buckets[f]` is the list of values that appeared exactly f times. Walking from `buckets[n]` down to `buckets[1]` collects the most-frequent values first, and we stop once we have k. That's O(n) total — linear, not log-linear. The cost is O(n) extra memory for the bucket array, but each bucket is usually sparse, so the real working set is small.\n\n\"For this problem I'd offer both — the heap is the textbook 'heap section' answer, but bucket sort is the asymptotically best answer because we're exploiting a *bound* on the values being sorted (frequencies ≤ n). That kind of structural exploit is what wins the bonus points.\"",
        "why": "Naming WHY bucket sort applies here — 'frequencies are bounded by n' — is what distinguishes derivation from memorization. Bucket sort isn't a general technique; it specifically works when the keys you're sorting have a small, known range. That's the case here (and in counting sort, radix sort, etc.). If the interviewer were to ask 'sort these arbitrary doubles by some property,' bucket sort wouldn't apply, and the answer would degrade back to the heap. Spelling out the precondition is the move."
      },
      {
        "title": "4. Trace through examples",
        "prompt": "Walk a few inputs to confirm the count-and-sort model.",
        "intro": "\"Let me trace through a few cases to confirm the model.\" Three inputs cover the standard mix-with-ties case, the degenerate single-value case, and an input with negatives to confirm the Map handles them.",
        "examples": [
          {
            "input": "nums=[1,1,1,2,2,3], k=2",
            "output": "[1, 2]",
            "note": "Textbook case — clear frequency ranking, no ties at the cutoff.",
            "trace": "count pass: 1→1, 1→2, 1→3, 2→1, 2→2, 3→1\nfinal count = Map { 1=>3, 2=>2, 3=>1 }\nentries = [[1,3], [2,2], [3,1]]\nsort desc by [1]: [[1,3], [2,2], [3,1]] (already sorted)\nslice(0, 2): [[1,3], [2,2]]\nmap → keys: [1, 2] ✓"
          },
          {
            "input": "nums=[1], k=1",
            "output": "[1]",
            "note": "Degenerate — single element, k=1.",
            "trace": "count pass: 1→1\nfinal count = Map { 1=>1 }\nentries = [[1,1]]\nsort: [[1,1]]\nslice(0, 1): [[1,1]]\nmap → keys: [1] ✓"
          },
          {
            "input": "nums=[4,1,-1,2,-1,2,3], k=2",
            "output": "[-1, 2]",
            "note": "Negatives + tie at frequency 2. Either [-1,2] or [2,-1] is correct under 'any order'; Map insertion order makes -1 come first since it was inserted earlier.",
            "trace": "count pass: 4→1, 1→1, -1→1, 2→1, -1→2, 2→2, 3→1\nfinal count = Map { 4=>1, 1=>1, -1=>2, 2=>2, 3=>1 }\nentries = [[4,1], [1,1], [-1,2], [2,2], [3,1]]\nsort desc by [1]: [[-1,2], [2,2], [4,1], [1,1], [3,1]]\n  (V8's sort is stable since 2019, so -1 stays before 2 — they tied at count 2 and -1 came first in entries)\nslice(0, 2): [[-1,2], [2,2]]\nmap → keys: [-1, 2] ✓"
          }
        ],
        "why": "The third trace is the load-bearing one — it surfaces *two* subtle behaviors: (1) Map.entries() preserves insertion order, which (2) combined with V8's stable sort means ties are broken by first-seen-in-the-input order. If the contract demanded a different tiebreaker (smaller value first, larger first, whatever), you'd add a secondary sort key. The fact that 'sort by count descending' happens to give a *deterministic* answer here is a happy accident of stability; calling it out shows you understand the mechanism rather than getting lucky."
      },
      {
        "title": "5. Edge cases to mention",
        "prompt": "What inputs would trip up a careless implementation?",
        "say": "\"Edges worth flagging:\n• k = 1 → return the single most-frequent value. Trivial; both approaches handle it.\n• k = number_of_distinct → return every distinct value. Sort version is fine; heap version's keep-only-k constraint becomes a no-op.\n• Ties at the k boundary → if frequencies 3,3,2,2 and k=3, the third spot is ambiguous between the two 2-counts. Either is correct under 'any order'; specify a tiebreaker if the contract demands one.\n• Negative numbers / zero → handled — Map keys accept any value. (`{}` would coerce to strings; Map preserves type, which is why we use it.)\n• Empty array → either return `[]` or rely on `slice(0, k)` of an empty entries array (which is `[]`). Defensive but the math degrades correctly.\n• Floating-point keys → Map handles equality on `===`, so `0.1 + 0.2` would key separately from `0.3`. Worth flagging if floats are in play.\"",
        "why": "The Map-vs-object distinction is the senior-tier callout — most candidates reach for `{}` reflexively, which coerces numeric keys to strings (`{1: 1}` and `{'1': 1}` are the same key). For an array of numbers, that's silently wrong if you later iterate and expect numeric keys back. The floating-point edge is the deepest cut: if the input could contain repeated computed floats, you'd need to normalize. Most candidates miss it entirely."
      },
      {
        "title": "6. Complexity & wrap-up",
        "prompt": "How would you defend the complexity claim and rank the three approaches?",
        "say": "\"Three approaches, three complexity profiles:\n\n• Sort-and-slice (the canonical): O(n) for counting + O(d log d) for sort = O(n log n) worst case (d ≤ n). Space O(d) for the Map. This is what I'd ship for general use.\n\n• Size-k min-heap keyed by frequency: O(n) counting + O(d log k) heap ops = O(n + d log k). Space O(k). Wins when k << d — the typical 'top 10 trending out of millions' case.\n\n• Bucket sort: O(n) counting + O(n) bucket build + O(n) walk = O(n) total. Space O(n) for the bucket array, mostly empty. Wins asymptotically because we exploit the bound 'frequency ≤ n.'\n\n\"The amortization defense for bucket sort is the prettiest of the three: each value contributes once to the count Map and lands in exactly one bucket. The walk from `buckets[n]` down to `buckets[1]` visits each bucket once, and the total number of values across all buckets is exactly d ≤ n. So the sum of work is O(n) — no log factor, period.\n\n\"If you wanted me to extend this: 'top k frequent *words*' is the same problem with string keys; 'top k frequent in a stream' is where the heap dominates because you can't bucket-sort an unbounded stream. The pattern — count-then-rank — generalizes anywhere you need partial order by frequency.\"",
        "why": "Offering all three approaches with their precondition for winning ('heap when k<<d, bucket when frequencies are bounded, sort otherwise') is what proves you see this as a *family* of problems, not a single answer. The bucket-sort amortization argument is the cleanest one to articulate: 'every element pays O(1) into a bucket, the walk visits every bucket once, total work is bounded by 2n.' That's the form of argument an interviewer wants to hear — concrete pay-per-element accounting, no hand-waving."
      }
    ]
  },
  "walkthrough": {
    "intro": "Step through the count-then-sort approach: build the frequency Map in one pass, sort entries descending by count, slice the top K, and project back to keys.",
    "examples": [
      { "label": "[1,1,1,2,2,3] k=2 → [1,2]", "input": { "nums": [1, 1, 1, 2, 2, 3], "k": 2 }, "expected": "[1,2]" },
      { "label": "[1] k=1 → [1] (degenerate single value)", "input": { "nums": [1], "k": 1 }, "expected": "[1]" },
      { "label": "[4,1,-1,2,-1,2,3] k=2 → [-1,2]", "input": { "nums": [4, 1, -1, 2, -1, 2, 3], "k": 2 }, "expected": "[-1,2]" }
    ],
    "trace": [
      "function* trace(input) {",
      "  const { nums, k } = input;",
      "  yield { line: 1, label: `enter topKFrequent(nums=${JSON.stringify(nums)}, k=${k})`, state: { nums, k } };",
      "  const count = new Map();",
      "  yield { line: 3, label: 'init count = new Map() (empty)', state: { nums, k, count: [] } };",
      "  for (let i = 0; i < nums.length; i++) {",
      "    const n = nums[i];",
      "    const prev = count.get(n) || 0;",
      "    count.set(n, prev + 1);",
      "    yield { line: 5, label: `n=${n}: count[${n}] = ${prev} + 1 = ${prev + 1}`, state: { nums, k, i, n, count: [...count.entries()] } };",
      "  }",
      "  const entries = [...count.entries()];",
      "  yield { line: 6, label: `entries from count → ${JSON.stringify(entries)}`, state: { nums, k, count: [...count.entries()], entries } };",
      "  const sorted = entries.slice().sort((a, b) => b[1] - a[1]);",
      "  yield { line: 8, label: `sort by b[1]-a[1] (descending count) → ${JSON.stringify(sorted)}`, state: { nums, k, entries, sorted } };",
      "  const sliced = sorted.slice(0, k);",
      "  yield { line: 9, label: `slice(0, ${k}) → top ${k}: ${JSON.stringify(sliced)}`, state: { nums, k, sorted, sliced } };",
      "  const result = sliced.map(([num]) => num);",
      "  yield { line: 10, label: `map([num]) => num → ${JSON.stringify(result)} (drop counts, keep keys)`, state: { nums, k, sliced, result } };",
      "  yield { line: 11, label: `return ${JSON.stringify(result)}`, state: { nums, k, result, returns: JSON.stringify(result) } };",
      "}"
    ]
  },
  "reference": {
    "code": "function topKFrequent(nums, k) {\n  // Map preserves numeric keys without string coercion + iterates in insertion order.\n  const count = new Map();\n  // Single O(n) pass: bump existing count or seed with 1 via the `|| 0` fallback.\n  for (const n of nums) count.set(n, (count.get(n) || 0) + 1);\n  return [...count.entries()]\n    // Descending by count — b[1] - a[1]; reverse operands for ascending.\n    .sort((a, b) => b[1] - a[1])\n    .slice(0, k)\n    .map(([num]) => num);\n}\n// Time O(n log n) via sort, Space O(n)\n// Optimal: O(n log k) with a size-K MIN-heap keyed by frequency.",
    "notes": [
      "Step 1 is always the same: build a frequency Map in one O(n) pass.",
      "Sort DESCENDING by count: (a, b) => b[1] - a[1]. Reversing the operands gives ascending.",
      "For O(n log k), push each entry into a min-heap of size K and pop when it overflows — keeps only the top K alive.",
      "Bucket sort by frequency is the third option: O(n) overall, but uses O(n) extra buckets."
    ]
  },
  "L1": {
    "questions": [
      {
        "q": "Why a Map for frequencies instead of a plain object?",
        "options": [
          "Maps allow numeric keys without string coercion and iterate in insertion order",
          "Map.get is O(1) while obj[key] is O(log n) because it traverses the prototype chain",
          "Objects skip inherited keys like `constructor` only when used as a Map",
          "Maps automatically deduplicate the input array as a side effect of `.set`"
        ],
        "answer": 0
      },
      {
        "q": "Which comparator sorts entries by count DESCENDING?",
        "options": [
          "(a, b) => a[1] - b[1]",
          "(a, b) => b[1] - a[1]",
          "(a, b) => a[0] - b[0]",
          "(a, b) => b[0] - a[0]"
        ],
        "answer": 1
      },
      {
        "q": "Time complexity of the sort-then-slice approach?",
        "options": [
          "O(n)",
          "O(n log k)",
          "O(n log n)",
          "O(n^2)"
        ],
        "answer": 2
      },
      {
        "q": "How does a min-heap of size K give O(n log k)?",
        "options": [
          "It sorts the array faster",
          "For each of n entries we push and possibly pop (both O(log k)), keeping only the top K alive",
          "Min-heaps are inherently O(1)",
          "It does not — it is also O(n log n)"
        ],
        "answer": 1
      },
      {
        "q": "Bucket sort solves Top K Frequent in O(n). What makes that linear time possible here?",
        "options": [
          "Bucket sort is always O(n) for any input",
          "A frequency can be at most n, so we can index buckets by count 0..n — a bounded key range, no comparison sort needed",
          "It skips the counting pass",
          "It uses a heap internally"
        ],
        "answer": 1,
        "explain": "Bucket sort is linear only when keys fall in a small known range; here a count cannot exceed n, so buckets[0..n] cover every possible frequency — no O(log) comparison sort required."
      },
      {
        "q": "In the bucket-sort approach, what does buckets[f] hold?",
        "options": [
          "The value f",
          "The list of values that each appear exactly f times",
          "The fth most frequent value",
          "A count of how many buckets are used"
        ],
        "answer": 1,
        "explain": "Index = frequency: buckets[f] collects every distinct value whose count equals f, so walking from high f down gathers the most frequent first."
      },
      {
        "q": "To collect the answer from the buckets, you walk:",
        "options": [
          "From buckets[0] upward until you have k values",
          "From buckets[n] down toward buckets[1], collecting values until you have k",
          "Only buckets[k]",
          "In random order"
        ],
        "answer": 1,
        "explain": "The most frequent values sit in the highest-index buckets, so you descend from buckets[n] and stop once k values are gathered."
      },
      {
        "q": "When does the size-k min-heap beat bucket sort in practice?",
        "options": [
          "Never — bucket sort always wins",
          "In a stream of unbounded length where you cannot pre-size an n+1 bucket array, the heap maintains the top k online",
          "When k equals the number of distinct values",
          "When all counts are equal"
        ],
        "answer": 1,
        "explain": "Bucket sort needs a bounded, known n to size the array; for an unbounded stream the size-k heap keeps the top k alive in O(log k) per element without that bound."
      },
      {
        "q": "The first step is identical across all three approaches (sort, heap, bucket). What is it?",
        "options": [
          "Sort nums ascending",
          "Build a frequency map in one O(n) pass",
          "Allocate a heap",
          "Remove duplicates"
        ],
        "answer": 1,
        "explain": "All three need value-to-count first; the count map is the shared O(n) preprocessing step, and only the ranking step after it differs."
      }
    ]
  },
  "L2": {
    "exercises": [
      {
        "prompt": "Fill in the count-and-sort Top K. The comparator direction is the trick.",
        "template": "function topKFrequent(nums, k) {\n  const count = new ___();\n  for (const n of nums) count.set(n, (count.get(n) || 0) + 1);\n  return [...count.entries()]\n    .sort((a, b) => ___[1] - ___[1])\n    .slice(0, k)\n    .map(([num]) => num);\n}\nconsole.log(JSON.stringify(topKFrequent([1, 1, 1, 2, 2, 3], 2)));",
        "blanks": [
          {
            "answer": "Map",
            "hint": "frequency table — value to count"
          },
          {
            "answer": "b",
            "hint": "descending: larger count first"
          },
          {
            "answer": "a",
            "hint": "second operand of the descending subtraction"
          }
        ],
        "expectedOutput": "[1,2]"
      }
    ]
  },
  "L3": {
    "prompt": "Write `topKFrequent(nums, k)` returning the K most-frequent values. Log `JSON.stringify(topKFrequent([1, 1, 1, 2, 2, 3], 2))`.",
    "expectedOutput": "[1,2]",
    "canonical": "function topKFrequent(nums, k) {\n  // Map preserves numeric keys without string coercion + iterates in insertion order.\n  const count = new Map();\n  // Single O(n) pass: bump existing count or seed with 1 via the `|| 0` fallback.\n  for (const n of nums) count.set(n, (count.get(n) || 0) + 1);\n  return [...count.entries()]\n    // Descending by count — b[1] - a[1]; reverse operands for ascending.\n    .sort((a, b) => b[1] - a[1])\n    .slice(0, k)\n    .map(([num]) => num);\n}\nconsole.log(JSON.stringify(topKFrequent([1, 1, 1, 2, 2, 3], 2)));",
    "hints": [
      "Count first, sort second, slice third.",
      "Descending comparator: (a, b) => b[1] - a[1].",
      "For O(n log k), swap the sort for a size-K min-heap keyed by frequency."
    ]
  }
}
