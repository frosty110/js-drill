{
  "id": "p-merge-two-sorted",
  "title": "Merge Two Sorted Lists",
  "section": "Linked List",
  "track": "patterns",
  "status": "full",
  "description": "Splice two sorted lists into one sorted list using a dummy head and a tail pointer. Iterative, O(1) extra space beyond the dummy.",
  "mechanics": ["linked-list-walk", "dummy-head-sentinel"],
  "reference": {
    "code": "function mergeTwoLists(a, b) {\n  const dummy = { val: 0, next: null };          // sentinel — simplifies edge cases\n  let tail = dummy;\n  while (a && b) {\n    if (a.val <= b.val) { tail.next = a; a = a.next; }\n    else                { tail.next = b; b = b.next; }\n    tail = tail.next;\n  }\n  tail.next = a || b;                            // attach whichever isn't exhausted\n  return dummy.next;\n}\n// Time O(n + m), Space O(1)",
    "notes": [
      "A dummy head removes the special case 'which list becomes the new head?'.",
      "Don't forget to attach the remaining tail — one list will still have nodes.",
      "Use <= (not <) for stability across equal values."
    ]
  },
  "L1": {
    "questions": [
      {
        "q": "Why use a dummy head node?",
        "options": [
          "Saves memory",
          "Removes the special case of picking the new head",
          "Required by JavaScript",
          "Improves time complexity"
        ],
        "answer": 1
      },
      {
        "q": "After the while loop, why do we set tail.next = a || b?",
        "options": [
          "To free memory",
          "One list may still have remaining sorted nodes to attach",
          "To detect cycles",
          "Stylistic only"
        ],
        "answer": 1
      },
      {
        "q": "Time complexity for lists of length n and m?",
        "options": [
          "O(n * m)",
          "O((n + m) log (n + m))",
          "O(n + m)",
          "O(max(n, m)^2)"
        ],
        "answer": 2
      },
      {
        "q": "What does the function return?",
        "options": [
          "dummy",
          "dummy.next",
          "tail",
          "a"
        ],
        "answer": 1,
        "explain": "dummy is the sentinel; the real merged head is dummy.next."
      }
    ]
  },
  "L2": {
    "exercises": [
      {
        "prompt": "Fill in the iterative merge using a dummy head.",
        "template": "function mergeTwoLists(a, b) {\n  const dummy = { val: 0, next: null };\n  let tail = ___;\n  while (a && b) {\n    if (a.val <= b.val) { tail.next = a; a = a.next; }\n    else                { tail.next = b; b = b.next; }\n    tail = tail.___;\n  }\n  tail.next = a || ___;\n  return dummy.___;\n}\nfunction toList(arr) { let d = { next: null }, c = d; for (const v of arr) { c.next = { val: v, next: null }; c = c.next; } return d.next; }\nfunction toArr(h) { const o = []; while (h) { o.push(h.val); h = h.next; } return o; }\nconsole.log(JSON.stringify(toArr(mergeTwoLists(toList([1, 2, 4]), toList([1, 3, 4])))));",
        "blanks": [
          {
            "answer": "dummy",
            "hint": "tail starts at the sentinel"
          },
          {
            "answer": "next",
            "hint": "advance the tail"
          },
          {
            "answer": "b",
            "hint": "whichever list still has nodes"
          },
          {
            "answer": "next",
            "hint": "skip the sentinel"
          }
        ],
        "expectedOutput": "[1,1,2,3,4,4]"
      }
    ]
  },
  "L3": {
    "prompt": "Write `mergeTwoLists(a, b)` merging two sorted singly linked lists. Use toList/toArr helpers and log `JSON.stringify(toArr(mergeTwoLists(toList([1, 2, 4]), toList([1, 3, 4]))))`.",
    "expectedOutput": "[1,1,2,3,4,4]",
    "canonical": "function mergeTwoLists(a, b) {\n  const dummy = { val: 0, next: null };\n  let tail = dummy;\n  while (a && b) {\n    if (a.val <= b.val) { tail.next = a; a = a.next; }\n    else                { tail.next = b; b = b.next; }\n    tail = tail.next;\n  }\n  tail.next = a || b;\n  return dummy.next;\n}\nfunction toList(arr) { let d = { next: null }, c = d; for (const v of arr) { c.next = { val: v, next: null }; c = c.next; } return d.next; }\nfunction toArr(h) { const o = []; while (h) { o.push(h.val); h = h.next; } return o; }\nconsole.log(JSON.stringify(toArr(mergeTwoLists(toList([1, 2, 4]), toList([1, 3, 4])))));",
    "hints": [
      "Use a dummy head; keep a tail pointer.",
      "After the loop, attach whichever list still has remaining nodes."
    ]
  }
}
