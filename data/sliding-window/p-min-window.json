{
  "id": "p-min-window",
  "title": "Min Window Substring",
  "section": "Sliding Window",
  "track": "patterns",
  "status": "full",
  "description": "Smallest substring of s containing every char of t (with multiplicities). Sliding window with need-Map + have/required counter. O(|s| + |t|).",
  "mechanics": ["sliding-window", "freq-map"],
  "conversation": {
    "intro": "How an interview about this problem would actually play out. Each section has two parts: what you'd actually say out loud, and why that move matters. Tap a section to expand.",
    "sections": [
      {
        "title": "1. Restate & clarify",
        "prompt": "Before touching code — pin down the inputs.",
        "say": "\"Let me make sure I have this right: I'm looking for the shortest contiguous substring of `s` that contains every character of `t`, counting multiplicities — so if `t = \"AABC\"`, the window has to have at least 2 A's, 1 B, and 1 C. If multiple windows tie on length, I return the leftmost one. If no window works, I return the empty string.\"\n\nThen a few quick clarifying questions:\n• Multiplicities — confirmed `t = \"AABC\"` requires two A's, not just one?\n• Case sensitivity — is `'a'` distinct from `'A'`?\n• Character set — ASCII, or full Unicode?\n• Tie-break — leftmost shortest, or any shortest?\n• What if `t` is empty — return `\"\"`, or undefined behavior?",
        "why": "The multiplicities question is the entire game. A candidate who treats this as 'window contains every distinct char of t' writes a much simpler (and wrong) solution — and the interviewer will hand them a test case like `s=\"a\", t=\"aa\"` to expose it. Asking up front signals you've already spotted the trap. The tie-break question also matters — the natural implementation gives leftmost-shortest for free, but it's good to confirm rather than discover it's wrong in a test."
      },
      {
        "title": "2. Brute force first (out loud)",
        "prompt": "Name the obvious O(|s|² · |t|) solution before you discard it.",
        "say": "\"The brute force is: for every pair of indices `(i, j)` with `i ≤ j`, check whether `s[i..j]` contains all of `t` with the right multiplicities — that check is O(|t|) using a frequency map. Three nested loops total — O(|s|² · |t|). Correct but obviously too slow.\n\nThe optimization comes from noticing that as I expand `j` to the right, I'm building on a window I already characterized. I shouldn't re-scan from scratch — I should maintain a frequency map of the current window incrementally and a single 'how close am I' signal. That points to two pointers with a counter.\"",
        "why": "Two reasons. (1) Stating the explicit O(|s|² · |t|) bound (not just 'slow') shows you can reason about complexity from the loop structure, not just recite memorized answers. (2) The optimization is non-obvious — going from 'check every window' to 'extend the window and amortize the count' is the whole insight. Naming the redundant work (re-scanning) makes the leap to 'maintain incrementally' feel inevitable rather than magical."
      },
      {
        "title": "3. Spot the pattern",
        "prompt": "What signal in the problem points to a variable-size window with a two-counter satisfaction signal?",
        "say": "\"This is variable-size sliding window — `l` and `r` move independently. The expand/shrink loop is: extend `r` until the window is valid (covers `t`), then shrink `l` while it stays valid, recording the smallest valid window seen. The valid → invalid → valid oscillation is exactly the variable-window template.\n\nThe load-bearing trick is the satisfaction signal — how do I cheaply know 'is this window valid right now?' The naive answer is 'compare two whole maps every iteration,' which makes the algorithm O(|s| · alphabet). The clever answer is a `have` counter that tracks how many DISTINCT chars in `t` have their quota met in the current window. The window is valid exactly when `have === required`, where `required = need.size`. That collapses the validity check from O(alphabet) to O(1).\n\nThe two `have` updates have a critical asymmetry — increment ONLY when `window[c]` hits `need[c]` exactly (`===`), and decrement ONLY when `window[c]` drops strictly below `need[c]`. Going past the quota on either side mustn't bump `have`, because the window is still satisfying that char.\"",
        "why": "The variable-size two-pointer window with a need/have counter is one of the most reused interview patterns — it shows up in 'Longest Substring with At Most K Distinct,' 'Permutation in String,' 'Find All Anagrams,' 'Substring with Concatenation of All Words.' Naming the asymmetric `===` and `<` conditions out loud isn't pedantry — it's the exact bug interviewers expect you to almost ship, and articulating it preempts the 'why not >= ?' pushback. The 'collapse the validity check to one int' insight is also what separates a working solution from an O(n · alphabet) almost-solution."
      },
      {
        "title": "4. Trace through examples",
        "prompt": "Walk a few inputs to confirm the model before writing the loop.",
        "intro": "\"Let me trace through a couple of cases to make sure my model is right.\" Three inputs cover the interesting behaviors — a small case with duplicates in t that exercises the multiplicities logic, an impossible case (the trap from clarifying questions), and the classic LeetCode example.",
        "examples": [
          {
            "input": "s=\"ABAC\", t=\"ABC\"",
            "output": "\"BAC\"",
            "note": "Small case — need={A:1, B:1, C:1}, required=3. The window 'ABA' has have=2 (A and B met), then C arrives and have=3, triggering shrinks until 'BAC' lands as smallest valid.",
            "trace": "need = {A:1, B:1, C:1}, required = 3\nr=0 c='A' → window={A:1}. window[A]=1 === need[A]=1 → have=1\nr=1 c='B' → window={A:1,B:1}. window[B]=1 === need[B]=1 → have=2\nr=2 c='A' → window={A:2,B:1}. window[A]=2 ≠ need[A]=1 (excess) → have stays 2\nr=3 c='C' → window={A:2,B:1,C:1}. window[C]=1 === need[C]=1 → have=3\n  have===required → bestLen=4 (\"ABAC\"), shrink:\n  drop s[0]='A' → window={A:1,B:1,C:1}. window[A]=1 still === need[A]=1 (not <), have stays 3\n  bestLen=3 (\"BAC\"), shrink again:\n  drop s[1]='B' → window={A:1,B:0,C:1}. window[B]=0 < need[B]=1 → have=2, l=2, loop ends\nreturn \"BAC\""
          },
          {
            "input": "s=\"a\", t=\"aa\"",
            "output": "\"\" (impossible)",
            "note": "The trap from clarifying — |s| < |t| early-return short-circuits before any work. Without it, the loop just finds no valid window and returns \"\" anyway.",
            "trace": "t.length=2, s.length=1 → s.length < t.length → early return \"\"\n\n(If the early return weren't there:\nneed = {a:2}, required = 1\nr=0 c='a' → window={a:1}. window[a]=1 ≠ need[a]=2 → have=0\nloop ends, bestLen=Infinity → return \"\")"
          },
          {
            "input": "s=\"ADOBECODEBANC\", t=\"ABC\"",
            "output": "\"BANC\"",
            "note": "Classic LeetCode case — the L3 prompt's own input. First valid window is \"ADOBEC\" (length 6), then \"CODEBA\"-ish intermediate, finally \"BANC\" (length 4) wins.",
            "trace": "need = {A:1, B:1, C:1}, required = 3\nr=0..4 'A','D','O','B','E' → window collects A and B, have=2\nr=5 c='C' → have=3. shrink: \"ADOBEC\" (len 6) is bestLen. drop A → have=2, l=1\nr=6..9 'O','D','E','B' → window={B:2,C:1,D:1,E:1,O:1,...}. have stays 2 (need A still missing)\nr=10 c='B' → already have B met, no change to have\nr=11 c='A' → window[A]=1 === need[A]=1 → have=3\n  shrink: window is s[1..11]=\"DOBECODEBA\" (len 10) — not better than 6. drop D, drop O, drop B → window[B]=1 still ok, drop E, drop C → window[C]=0 < need[C]=1 → have=2, l=6\nr=12 c='C' → window[C]=1 === need[C]=1 → have=3\n  shrink: window is s[6..12]=\"ODEBANC\" (len 7) → not better than 6. drop O, drop D, drop E → none in need, have stays 3\n  window is s[9..12]=\"BANC\" (len 4) → new bestLen=4, bestL=9\n  drop B → window[B]=0 < need[B]=1 → have=2, l=10, loop ends\nreturn \"BANC\""
          }
        ],
        "why": "The 'ABAC' trace surfaces the most important bug — when shrinking, dropping the first 'A' takes window[A] from 2 to 1, which still meets need[A]=1, so `have` must NOT decrement. Only when window[B] drops from 1 to 0 (below need[B]=1) does `have` decrement. Watching that play out in a concrete trace is what fixes the asymmetric condition in your head; without the trace, the `<` vs `<=` choice feels arbitrary. The 'a'/'aa' impossible case defends the early return — without it, you still get the right answer, but it's a cheap O(1) guard that saves a pass over s in degenerate inputs."
      },
      {
        "title": "5. Edge cases to mention",
        "prompt": "What inputs would trip up a careless implementation?",
        "say": "\"Before I declare it done, let me think about edges:\n• `t` empty → spec usually says return `\"\"`. Worth confirming and guarding (early return).\n• `|s| < |t|` → impossible by pigeonhole, return `\"\"`. Cheap early-return.\n• `s === t` → the whole string is the answer, runs in O(n). Good sanity check.\n• `t` has duplicates like `\"aabbc\"` — this is the multiplicities trap. A candidate using a Set instead of a Map silently fails here. Worth saying out loud.\n• `t` larger than s but has all needed chars individually — still impossible (pigeonhole again). The early return covers it.\n• Unicode/emoji in s or t — `s[r]` indexes UTF-16 code units, so a multi-unit emoji reads as halves. For interview, assume ASCII unless asked. The fix would be iterating with `for...of` and using the codepoint as the map key.\n• Massive `s`, tiny `t` — still O(|s| + |t|). The amortization holds.\"",
        "why": "The multiplicities edge is THE failure mode here. Other window problems (Longest Substring Without Repeating) get away with a Set; this one absolutely needs counts. Naming the Set-vs-Map distinction unprompted shows you've internalized why the data structure changes with the problem variant — that's pattern fluency, not pattern memorization. The empty/short-`t` guards also signal defensive thinking — a candidate who skips them often gets bitten by an undefined behavior in `need.size === 0` (required=0 makes `have === required` true forever, returning the empty prefix as 'valid')."
      },
      {
        "title": "6. Complexity & wrap-up",
        "prompt": "How would you defend the complexity claim?",
        "say": "\"Time is O(|s| + |t|) — building `need` from `t` is O(|t|), then the main loop: `r` advances exactly |s| times, and `l` advances at most |s| times across the whole run (it only moves forward, never back). So the total work in the outer for and the inner while combined is bounded by 2|s|. Each character of `s` enters the window once and leaves once. Same amortization argument as the longest-substring-no-repeats lesson.\n\nSpace is O(|s| + |t|) in the worst case — `need` is bounded by |t|, `window` is bounded by |s|. If the alphabet is small (ASCII), both collapse to O(|alphabet|) ≈ O(1).\n\nThe extension worth naming: 'Find All Anagrams of `p` in `s`' uses the same need/have skeleton, but with a FIXED window size of |p| — you don't shrink based on validity, you shrink whenever `r - l + 1 > |p|`. Same template, different shrink trigger. 'Permutation in String' (LeetCode 567) is the boolean variant of that. The template generalizes to any 'window must contain pattern' question.\"",
        "why": "The amortization argument — `l` moves at most |s| times across the whole run — is the right defense against an interviewer who points at the nested while and says 'isn't that O(|s|²)?' Naming Find All Anagrams and Permutation in String as siblings proves you see the template, not just this problem. The fixed-vs-variable-window distinction at the end is the meta-pattern that ties this lesson to Sliding Window Maximum (also fixed-size) — useful framing for the whole section."
      }
    ]
  },
  "walkthrough": {
    "intro": "Step through the canonical need/have sliding window on a real input. The highlighted line is what just ran; the state panel shows the locals after that step. Use Prev/Next, or pick a different example.",
    "examples": [
      { "label": "s=\"ABAC\", t=\"ABC\" → \"BAC\"",          "input": { "s": "ABAC", "t": "ABC" },          "expected": "BAC" },
      { "label": "s=\"a\", t=\"aa\" → \"\" (impossible)",     "input": { "s": "a", "t": "aa" },             "expected": "" },
      { "label": "s=\"ADOBECODEBANC\", t=\"ABC\" → \"BANC\"", "input": { "s": "ADOBECODEBANC", "t": "ABC" }, "expected": "BANC" }
    ],
    "trace": [
      "function* trace(input) {",
      "  const s = input.s;",
      "  const t = input.t;",
      "  yield { line: 1, label: `enter function — s=\"${s}\", t=\"${t}\"`, state: { s, t } };",
      "  if (t.length === 0 || s.length < t.length) {",
      "    yield { line: 2, label: `t.length=${t.length}, s.length=${s.length} — early return \"\" (impossible)`, state: { s, t, returns: '' } };",
      "    return;",
      "  }",
      "  const need = new Map();",
      "  for (const c of t) need.set(c, (need.get(c) || 0) + 1);",
      "  yield { line: 5, label: `build need from t → ${JSON.stringify([...need.entries()])}`, state: { s, t, need: [...need.entries()] } };",
      "  const required = need.size;",
      "  const window = new Map();",
      "  let have = 0, l = 0, bestL = 0, bestLen = Infinity;",
      "  yield { line: 9, label: `init: required=${required}, have=0, l=0, bestLen=Infinity`, state: { s, t, need: [...need.entries()], window: [], required, have, l, bestL, bestLen: 'Infinity' } };",
      "  for (let r = 0; r < s.length; r++) {",
      "    const c = s[r];",
      "    window.set(c, (window.get(c) || 0) + 1);",
      "    yield { line: 12, label: `r=${r}, c='${c}' — window[${c}] = ${window.get(c)}`, state: { s, t, need: [...need.entries()], window: [...window.entries()], required, have, l, r, bestL, bestLen: bestLen === Infinity ? 'Infinity' : bestLen } };",
      "    if (need.has(c) && window.get(c) === need.get(c)) {",
      "      have++;",
      "      yield { line: 14, label: `window['${c}']=${window.get(c)} hit need['${c}']=${need.get(c)} → have=${have}`, state: { s, t, need: [...need.entries()], window: [...window.entries()], required, have, l, r, bestL, bestLen: bestLen === Infinity ? 'Infinity' : bestLen } };",
      "    }",
      "    while (have === required) {",
      "      yield { line: 16, label: `have(${have}) === required(${required}) → try to shrink window [${l},${r}]`, state: { s, t, need: [...need.entries()], window: [...window.entries()], required, have, l, r, bestL, bestLen: bestLen === Infinity ? 'Infinity' : bestLen } };",
      "      if (r - l + 1 < bestLen) {",
      "        bestLen = r - l + 1;",
      "        bestL = l;",
      "        yield { line: 17, label: `new best: bestL=${bestL}, bestLen=${bestLen} (\"${s.slice(bestL, bestL + bestLen)}\")`, state: { s, t, need: [...need.entries()], window: [...window.entries()], required, have, l, r, bestL, bestLen } };",
      "      }",
      "      const lc = s[l];",
      "      window.set(lc, window.get(lc) - 1);",
      "      yield { line: 19, label: `drop s[${l}]='${lc}' → window[${lc}]=${window.get(lc)}`, state: { s, t, need: [...need.entries()], window: [...window.entries()], required, have, l, r, bestL, bestLen } };",
      "      if (need.has(lc) && window.get(lc) < need.get(lc)) {",
      "        have--;",
      "        yield { line: 21, label: `window['${lc}']=${window.get(lc)} dropped below need['${lc}']=${need.get(lc)} → have=${have}`, state: { s, t, need: [...need.entries()], window: [...window.entries()], required, have, l, r, bestL, bestLen } };",
      "      }",
      "      l++;",
      "    }",
      "  }",
      "  const result = bestLen === Infinity ? '' : s.slice(bestL, bestL + bestLen);",
      "  yield { line: 25, label: `return ${bestLen === Infinity ? '\"\" (no window found)' : '\"' + result + '\"'}`, state: { s, t, need: [...need.entries()], window: [...window.entries()], required, have, l, bestL, bestLen: bestLen === Infinity ? 'Infinity' : bestLen, returns: result } };",
      "}"
    ]
  },
  "reference": {
    "code": "function minWindow(s, t) {\n  if (t.length === 0 || s.length < t.length) return \"\";\n  // need[c] = required count of c in any valid window.\n  const need = new Map();\n  for (const c of t) need.set(c, (need.get(c) || 0) + 1);\n  // have = DISTINCT chars whose window count has hit their need quota. required = distinct chars in t.\n  const required = need.size;\n  const window = new Map();\n  let have = 0, l = 0, bestL = 0, bestLen = Infinity;\n  for (let r = 0; r < s.length; r++) {\n    const c = s[r];\n    window.set(c, (window.get(c) || 0) + 1);\n    // === (not >=): going past the quota mustn't bump have again.\n    if (need.has(c) && window.get(c) === need.get(c)) have++;\n    // Window satisfies t — shrink from left while it still does, recording the best.\n    while (have === required) {\n      if (r - l + 1 < bestLen) { bestLen = r - l + 1; bestL = l; }\n      const lc = s[l];\n      window.set(lc, window.get(lc) - 1);\n      // Only decrement have when we drop BELOW the quota (excess removals don't break the window).\n      if (need.has(lc) && window.get(lc) < need.get(lc)) have--;\n      l++;\n    }\n  }\n  return bestLen === Infinity ? \"\" : s.slice(bestL, bestL + bestLen);\n}\n// Time O(|s| + |t|), Space O(|s| + |t|)",
    "notes": [
      "have counts DISTINCT chars whose window count reached the need count — not total chars. required = need.size.",
      "Only increment have when window[c] hits need[c] EXACTLY (===). Going above does not change have.",
      "Decrement have when window[lc] drops BELOW need[lc] after shrinking — only then has the window stopped satisfying t."
    ]
  },
  "L1": {
    "questions": [
      {
        "q": "What does the `have` counter track?",
        "options": [
          "Total chars in the window",
          "Distinct chars whose window count has reached the required need count",
          "Total chars of t seen so far",
          "Length of the current window"
        ],
        "answer": 1
      },
      {
        "q": "Why increment have only when window[c] === need[c] (not >=)?",
        "options": [
          "Performance",
          "Otherwise duplicate occurrences would inflate have past required and falsely trigger contract",
          "JavaScript map quirk",
          "It is wrong — should be >="
        ],
        "answer": 1
      },
      {
        "q": "When does have get decremented during the contract loop?",
        "options": [
          "Every iteration",
          "Only when window[lc] drops strictly BELOW need[lc] after decrement",
          "When l reaches r",
          "Never — it only goes up"
        ],
        "answer": 1
      },
      {
        "q": "What does the function return when no window works?",
        "options": [
          "null",
          "-1",
          "The empty string \"\"",
          "undefined"
        ],
        "answer": 2
      },
      {
        "q": "Time complexity?",
        "options": [
          "O(|s| * |t|)",
          "O(|s| + |t|) — each index of s is visited by l and r at most once",
          "O(|s|^2)",
          "O(|s| log |s|)"
        ],
        "answer": 1
      }
    ]
  },
  "L2": {
    "exercises": [
      {
        "prompt": "Fill in the need/have sliding window.",
        "template": "function minWindow(s, t) {\n  if (t.length === 0 || s.length < t.length) return \"\";\n  const need = new Map();\n  for (const c of t) need.set(c, (need.get(c) || 0) + 1);\n  const required = need.___;\n  const window = new Map();\n  let have = 0, l = 0, bestL = 0, bestLen = Infinity;\n  for (let r = 0; r < s.length; r++) {\n    const c = s[r];\n    window.set(c, (window.get(c) || 0) + 1);\n    if (need.has(c) && window.get(c) ___ need.get(c)) have++;\n    while (have === required) {\n      if (r - l + 1 < bestLen) { bestLen = r - l + 1; bestL = l; }\n      const lc = s[l];\n      window.set(lc, window.get(lc) - 1);\n      if (need.has(lc) && window.get(lc) ___ need.get(lc)) have--;\n      l++;\n    }\n  }\n  return bestLen === Infinity ? \"\" : s.slice(bestL, bestL + ___);\n}\nconsole.log(minWindow(\"ADOBECODEBANC\", \"ABC\"));",
        "blanks": [
          {
            "answer": "size",
            "hint": "number of DISTINCT chars in t"
          },
          {
            "answer": "===",
            "hint": "increment have only when count exactly matches need"
          },
          {
            "answer": "<",
            "hint": "decrement have only when count drops BELOW need"
          },
          {
            "answer": "bestLen",
            "hint": "slice end = start + length"
          }
        ],
        "expectedOutput": "BANC"
      }
    ]
  },
  "L3": {
    "prompt": "Write `minWindow(s, t)` returning the smallest substring of s containing every char of t (with multiplicities), or \"\" if impossible. Log `minWindow(\"ADOBECODEBANC\",\"ABC\")` then `JSON.stringify(minWindow(\"a\",\"aa\"))`.",
    "expectedOutput": "BANC\n\"\"",
    "canonical": "function minWindow(s, t) {\n  if (t.length === 0 || s.length < t.length) return \"\";\n  // need[c] = required count of c in any valid window.\n  const need = new Map();\n  for (const c of t) need.set(c, (need.get(c) || 0) + 1);\n  // have = DISTINCT chars whose window count has hit their need quota. required = distinct chars in t.\n  const required = need.size;\n  const window = new Map();\n  let have = 0, l = 0, bestL = 0, bestLen = Infinity;\n  for (let r = 0; r < s.length; r++) {\n    const c = s[r];\n    window.set(c, (window.get(c) || 0) + 1);\n    // === (not >=): going past the quota mustn't bump have again.\n    if (need.has(c) && window.get(c) === need.get(c)) have++;\n    // Window satisfies t — shrink from left while it still does, recording the best.\n    while (have === required) {\n      if (r - l + 1 < bestLen) { bestLen = r - l + 1; bestL = l; }\n      const lc = s[l];\n      window.set(lc, window.get(lc) - 1);\n      // Only decrement have when we drop BELOW the quota (excess removals don't break the window).\n      if (need.has(lc) && window.get(lc) < need.get(lc)) have--;\n      l++;\n    }\n  }\n  return bestLen === Infinity ? \"\" : s.slice(bestL, bestL + bestLen);\n}\nconsole.log(minWindow(\"ADOBECODEBANC\", \"ABC\"));\nconsole.log(JSON.stringify(minWindow(\"a\", \"aa\")));",
    "hints": [
      "Two Maps: need (counts from t) and window (counts in current [l, r]).",
      "have/required is the satisfaction signal — increment have only when window[c] hits need[c] exactly.",
      "Contract from the left while have === required, recording (l, length) whenever it beats bestLen."
    ],
    "criticalLines": [14, 21]
  }
}
