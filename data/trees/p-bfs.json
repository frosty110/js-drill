{
  "id": "p-bfs",
  "title": "BFS Level Order",
  "section": "Trees",
  "track": "patterns",
  "status": "full",
  "description": "BFS with a queue, processing one level per outer iteration by snapshotting the queue size before the inner loop.",
  "mechanics": ["bfs-level-snapshot", "array-as-queue"],
  "reference": {
    "code": "function levelOrder(root) {\n  if (!root) return [];\n  const res = [];\n  const queue = [root];\n  while (queue.length) {\n    const size = queue.length;          // snapshot — only THIS level\n    const level = [];\n    for (let i = 0; i < size; i++) {\n      const node = queue.shift();\n      level.push(node.val);\n      if (node.left)  queue.push(node.left);\n      if (node.right) queue.push(node.right);\n    }\n    res.push(level);\n  }\n  return res;\n}\n// Time O(n), Space O(w) where w is max level width",
    "notes": [
      "Snapshot queue.length BEFORE the inner loop — children pushed during the loop belong to the NEXT level.",
      "Guard the root: if root is null return [] (not [[]]).",
      "Array.shift is O(n); for very large trees prefer a head index or a real deque."
    ]
  },
  "L1": {
    "questions": [
      {
        "q": "Why snapshot queue.length into `size` before the inner loop?",
        "options": [
          "Performance only",
          "Because children pushed during the loop belong to the next level",
          "JavaScript requires it",
          "Avoids a TypeError"
        ],
        "answer": 1
      },
      {
        "q": "Time complexity?",
        "options": [
          "O(log n)",
          "O(n) — each node enqueued and dequeued once",
          "O(n log n)",
          "O(n^2)"
        ],
        "answer": 1
      },
      {
        "q": "For a null root, the function returns:",
        "options": [
          "[[]]",
          "[]",
          "null",
          "[null]"
        ],
        "answer": 1
      },
      {
        "q": "Which data structure is BFS built on?",
        "options": [
          "Stack (LIFO)",
          "Queue (FIFO)",
          "Priority queue",
          "Hash map"
        ],
        "answer": 1
      }
    ]
  },
  "L2": {
    "exercises": [
      {
        "prompt": "Fill in the level-order BFS.",
        "template": "function levelOrder(root) {\n  if (!root) return ___;\n  const res = [];\n  const queue = [root];\n  while (queue.length) {\n    const size = queue.___;\n    const level = [];\n    for (let i = 0; i < size; i++) {\n      const node = queue.___();\n      level.push(node.val);\n      if (node.left)  queue.push(node.___);\n      if (node.right) queue.push(node.right);\n    }\n    res.push(___);\n  }\n  return res;\n}\nconst root = { val: 3, left: { val: 9, left: null, right: null }, right: { val: 20, left: { val: 15, left: null, right: null }, right: { val: 7, left: null, right: null } } };\nconsole.log(JSON.stringify(levelOrder(root)));",
        "blanks": [
          {
            "answer": "[]",
            "hint": "empty tree → empty list of levels"
          },
          {
            "answer": "length",
            "hint": "snapshot the level size"
          },
          {
            "answer": "shift",
            "hint": "FIFO: take from the front"
          },
          {
            "answer": "left",
            "hint": "enqueue left child"
          },
          {
            "answer": "level",
            "hint": "append the completed level"
          }
        ],
        "expectedOutput": "[[3],[9,20],[15,7]]"
      }
    ]
  },
  "L3": {
    "prompt": "Write `levelOrder(root)` returning BFS level-order traversal as array of arrays. Log `JSON.stringify(levelOrder(root))` for the tree [3, 9, 20, null, null, 15, 7].",
    "expectedOutput": "[[3],[9,20],[15,7]]",
    "canonical": "function levelOrder(root) {\n  if (!root) return [];\n  const res = [];\n  const queue = [root];\n  while (queue.length) {\n    const size = queue.length;\n    const level = [];\n    for (let i = 0; i < size; i++) {\n      const node = queue.shift();\n      level.push(node.val);\n      if (node.left) queue.push(node.left);\n      if (node.right) queue.push(node.right);\n    }\n    res.push(level);\n  }\n  return res;\n}\nconst root = { val: 3, left: { val: 9, left: null, right: null }, right: { val: 20, left: { val: 15, left: null, right: null }, right: { val: 7, left: null, right: null } } };\nconsole.log(JSON.stringify(levelOrder(root)));",
    "hints": [
      "Queue + snapshot of its length before the inner loop.",
      "Push left then right children; collect values per level."
    ]
  }
}
