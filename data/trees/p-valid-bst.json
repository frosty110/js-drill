{
  "id": "p-valid-bst",
  "title": "Validate BST",
  "section": "Trees",
  "track": "patterns",
  "status": "full",
  "description": "Validate a BST by passing tightening (lo, hi) bounds down: every node must satisfy lo < node.val < hi. Recurse left with hi=node.val, right with lo=node.val. O(n).",
  "mechanics": ["tree-recursion"],
  "reference": {
    "code": "function isValidBST(root) {\n  // Carry the open interval (lo, hi) each node must fall inside — strict inequalities.\n  function check(node, lo, hi) {\n    if (!node) return true;\n    if (node.val <= lo || node.val >= hi) return false;\n    // Left subtree caps hi at node.val; right subtree raises lo to node.val.\n    return check(node.left, lo, node.val) && check(node.right, node.val, hi);\n  }\n  // Root has no bounds — start with ±Infinity sentinels.\n  return check(root, -Infinity, Infinity);\n}\n// Time O(n), Space O(h) recursion stack",
    "notes": [
      "Bounds approach beats in-order-and-compare: handles duplicates cleanly and never allocates the traversal array.",
      "Start with lo=-Infinity, hi=Infinity — the root has no constraint; tighten ONE side per recursion.",
      "Use strict <= and >= — a BST disallows equal values (unless the problem says otherwise)."
    ]
  },
  "L1": {
    "questions": [
      {
        "q": "What bounds does the left child of a node with value v inherit from a parent (lo, hi)?",
        "options": [
          "(lo, v)",
          "(v, hi)",
          "(lo, hi)",
          "(-Infinity, Infinity)"
        ],
        "answer": 0,
        "explain": "Going left tightens the UPPER bound to v; lo is unchanged."
      },
      {
        "q": "Why is the bounds approach preferred over in-order-traversal-and-compare?",
        "options": [
          "Faster asymptotically",
          "Handles duplicates cleanly and avoids allocating the traversal list",
          "In-order traversal is wrong on BSTs",
          "It uses less recursion depth"
        ],
        "answer": 1
      },
      {
        "q": "Initial call uses which bounds?",
        "options": [
          "(0, n)",
          "(-Infinity, Infinity)",
          "(root.val, root.val)",
          "(null, null)"
        ],
        "answer": 1
      },
      {
        "q": "Time and space complexity?",
        "options": [
          "O(n log n) time, O(1) space",
          "O(n) time, O(h) recursion stack",
          "O(n^2) time, O(n) space",
          "O(log n) time, O(log n) space"
        ],
        "answer": 1
      }
    ]
  },
  "L2": {
    "exercises": [
      {
        "prompt": "Fill in the bounds-check validator. Going LEFT tightens hi; going RIGHT tightens lo.",
        "template": "function isValidBST(root) {\n  function check(node, lo, hi) {\n    if (!node) return true;\n    if (node.val <= lo || node.val ___ hi) return false;\n    return check(node.left, lo, ___) && check(node.right, ___, hi);\n  }\n  return check(root, -Infinity, ___);\n}\nconst root = { val: 2, left: { val: 1, left: null, right: null }, right: { val: 3, left: null, right: null } };\nconsole.log(isValidBST(root));",
        "blanks": [
          {
            "answer": ">=",
            "hint": "BST is strict — equal to the upper bound is invalid"
          },
          {
            "answer": "node.val",
            "hint": "left subtree: tighten the UPPER bound"
          },
          {
            "answer": "node.val",
            "hint": "right subtree: tighten the LOWER bound"
          },
          {
            "answer": "Infinity",
            "hint": "no upper bound at the root"
          }
        ],
        "expectedOutput": "true"
      }
    ]
  },
  "L3": {
    "prompt": "Write `isValidBST(root)` using the (lo, hi) bounds approach. Build a valid BST [2,1,3] and an invalid BST [5,1,4,null,null,3,6]. Log both.",
    "expectedOutput": "true\nfalse",
    "canonical": "function isValidBST(root) {\n  // Carry the open interval (lo, hi) each node must fall inside — strict inequalities.\n  function check(node, lo, hi) {\n    if (!node) return true;\n    if (node.val <= lo || node.val >= hi) return false;\n    // Left subtree caps hi at node.val; right subtree raises lo to node.val.\n    return check(node.left, lo, node.val) && check(node.right, node.val, hi);\n  }\n  // Root has no bounds — start with ±Infinity sentinels.\n  return check(root, -Infinity, Infinity);\n}\nconst good = { val: 2, left: { val: 1, left: null, right: null }, right: { val: 3, left: null, right: null } };\nconst bad  = { val: 5, left: { val: 1, left: null, right: null }, right: { val: 4, left: { val: 3, left: null, right: null }, right: { val: 6, left: null, right: null } } };\nconsole.log(isValidBST(good));\nconsole.log(isValidBST(bad));",
    "hints": [
      "Pass (lo, hi) down — never compare to the parent directly.",
      "Left child inherits (lo, node.val); right child inherits (node.val, hi).",
      "In [5,1,4,null,null,3,6]: node 3 is in 4 left subtree but smaller than ancestor 5 — bounds catch this; naive parent-only check does not."
    ]
  }
}
