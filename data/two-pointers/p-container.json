{
  "id": "p-container",
  "title": "Container w/ Most Water",
  "section": "Two Pointers",
  "track": "patterns",
  "status": "full",
  "description": "Two pointers at the ends. Water is bounded by the shorter side, so always move the shorter pointer inward. O(n).",
  "mechanics": ["two-pointer"],
  "conversation": {
    "intro": "How an interview about this problem would actually play out. Each section has two parts: what you'd actually say out loud, and why that move matters. Tap a section to expand.",
    "sections": [
      {
        "title": "1. Restate & clarify",
        "prompt": "Before touching code — pin down the inputs.",
        "say": "\"Let me make sure I have this right: I'm given a `height` array where each value is a vertical line on the x-axis at that index. I pick two lines and form a container with the x-axis as the base — water sits inside up to the level of the shorter of the two lines. I return the maximum water area I can hold by choosing the two best lines.\"\n\nThen a few quick clarifying questions:\n• What's `height.length` — at least 2? (Need two lines to form a container.)\n• Can heights be 0? Negative? (Zero means a wall with no height; negative makes no physical sense.)\n• What's the largest height? (Affects whether `h * (r - l)` could overflow — for normal int ranges in JS we're safe inside `Number.MAX_SAFE_INTEGER`.)\n• Return just the area as a number, right?",
        "why": "The geometry of the problem is the part most people misread on the first pass — confusing it with histogram-style 'water trapped above the bars' (which is the Trapping Rain Water problem, totally different). Restating it with 'lines as walls, water bounded by shorter, base is x-axis distance' makes the model unambiguous and prevents writing the wrong algorithm."
      },
      {
        "title": "2. Brute force first (out loud)",
        "prompt": "Name the obvious O(n²) solution before you discard it.",
        "say": "\"The brute force is: nested loops over every pair `(i, j)` with `j > i`, compute `area = min(height[i], height[j]) * (j - i)`, track the max. That's O(n²) — for n up to ~10⁵ it's too slow. Let me see if there's a way to skip whole ranges of pairs without checking them.\"",
        "why": "Two reasons. (1) The brute force makes the area formula explicit — `min(h[i], h[j]) * (j - i)` — so when I describe the optimization, the interviewer already has the formula in their head. (2) It frames the optimization as 'how do I eliminate pairs from consideration without checking them?' which is exactly what the shorter-pointer move does."
      },
      {
        "title": "3. Spot the pattern",
        "prompt": "What signal in the problem points to two pointers converging?",
        "say": "\"Two-pointer convergence — but the trigger isn't just 'pair of indices on an array.' Here's the real argument: start with `l=0` and `r=n-1`, the widest possible base. Width is `r - l`, height is `min(h[l], h[r])`. Now whichever pointer I move inward, the width strictly decreases by 1. So for the new pair to beat the old area, the height has to go *up*. The shorter side is the bottleneck — if I move the *taller* side inward, the new min is at best the same shorter value, but width has shrunk, so area can only decrease. If I move the *shorter* side inward, there's at least a chance the new height exceeds the old shorter side and beats the old area. So: always move the shorter pointer. That eliminates every pair where the taller side was the one that could have moved, which is exactly the pairs we don't need to check.\"",
        "why": "This is the canonical 'greedy choice that's actually optimal' argument and it's the entire problem. The naming 'width-bounded by `r-l`, height bounded by `min(h[l], h[r])`, moving the taller pointer can only shrink width without raising height' is the senior derivation — most candidates just say 'move the shorter side' as a rule without explaining why moving the taller is provably useless. The 'why' is what separates someone who's memorized the solution from someone who'd derive it on a problem they hadn't seen."
      },
      {
        "title": "4. Trace through examples",
        "prompt": "Walk a few inputs to confirm the model before writing the loop.",
        "intro": "\"Let me trace through a couple of cases to make sure my model is right.\" Three inputs cover the interesting behaviors — the canonical mixed-heights case, the minimal-length case, and a case where the best container is at the very ends (so the algorithm shouldn't move past them).",
        "examples": [
          {
            "input": "[1, 8, 6, 2, 5, 4, 8, 3, 7]",
            "output": "49",
            "note": "Classic case — best container is height[1]=8 paired with height[8]=7, area=min(8,7)*(8-1)=49.",
            "trace": "init l=0 r=8 best=0\nl=0 r=8: h=min(1,7)=1, area=1*8=8 → best=8. h[l]=1<h[r]=7, l++ → l=1\nl=1 r=8: h=min(8,7)=7, area=7*7=49 → best=49. h[l]=8≥h[r]=7, r-- → r=7\nl=1 r=7: h=min(8,3)=3, area=3*6=18, keep best=49. h[l]=8≥h[r]=3, r-- → r=6\nl=1 r=6: h=min(8,8)=8, area=8*5=40, keep best=49. h[l]=8≥h[r]=8 (equal), r-- → r=5\nl=1 r=5: h=min(8,4)=4, area=4*4=16, keep best=49. h[l]=8≥h[r]=4, r-- → r=4\nl=1 r=4: h=min(8,5)=5, area=5*3=15, keep best=49. h[l]=8≥h[r]=5, r-- → r=3\nl=1 r=3: h=min(8,2)=2, area=2*2=4, keep best=49. h[l]=8≥h[r]=2, r-- → r=2\nl=1 r=2: h=min(8,6)=6, area=6*1=6, keep best=49. h[l]=8≥h[r]=6, r-- → r=1\nl<r false, exit. return 49"
          },
          {
            "input": "[1, 1]",
            "output": "1",
            "note": "Minimal case — only one pair possible. Loop runs once, exits immediately.",
            "trace": "init l=0 r=1 best=0\nl=0 r=1: h=min(1,1)=1, area=1*1=1 → best=1. h[l]=1≥h[r]=1, r-- → r=0\nl<r false (0<0), exit. return 1"
          },
          {
            "input": "[4, 3, 2, 1, 4]",
            "output": "16",
            "note": "Best at the ends — the algorithm catches it on the first iteration. If it kept moving past, it'd never find anything better.",
            "trace": "init l=0 r=4 best=0\nl=0 r=4: h=min(4,4)=4, area=4*4=16 → best=16. h[l]=4≥h[r]=4 (equal), r-- → r=3\nl=0 r=3: h=min(4,1)=1, area=1*3=3, keep best=16. h[l]=4≥h[r]=1, r-- → r=2\nl=0 r=2: h=min(4,2)=2, area=2*2=4, keep best=16. h[l]=4≥h[r]=2, r-- → r=1\nl=0 r=1: h=min(4,3)=3, area=3*1=3, keep best=16. h[l]=4≥h[r]=3, r-- → r=0\nl<r false, exit. return 16"
          }
        ],
        "why": "Two things the trace exposes. (1) On the equal-heights case (`h[l] === h[r]`), the `<` check in `if (height[l] < height[r])` falls through to `r--` — that's fine because moving either pointer gives a symmetric outcome; both candidates would tie. Naming this out loud reassures the interviewer that you didn't miss the tie. (2) The `[4,3,2,1,4]` case confirms the algorithm correctly identifies the optimal pair on iteration 1 and doesn't fail to find it later — a sanity check that 'always move the shorter side' isn't quietly leaving optimal pairs unconsidered."
      },
      {
        "title": "5. Edge cases to mention",
        "prompt": "What inputs would trip up a careless implementation?",
        "say": "\"Before I declare it done, let me think about edges:\n• Length 2 → exactly one container possible, loop runs once and exits. ✓\n• Length 0 or 1 → `r = length - 1` is -1 or 0, `l < r` is false at entry, return best=0. (Problem usually constrains length ≥ 2, but defensive.)\n• All-same heights `[5,5,5,5]` → best is `5 * (n-1)`, found on the first iteration, then never beaten. The shorter-side rule degenerates to 'always r--' (or always l++); doesn't matter, every subsequent area is smaller because width shrinks while h stays at 5.\n• One zero in the middle `[3, 0, 3]` → the 0-height bar doesn't poison the algorithm because we don't have to pick it; the answer is `min(3,3)*2 = 6`.\n• A zero at one end `[0, 5, 5, 5]` → first iteration has h=0, area=0; the shorter-side rule moves l++ past the zero immediately, and we proceed with a meaningful container.\n• Strictly increasing `[1,2,3,4,5]` or decreasing `[5,4,3,2,1]` → in the increasing case, every iteration moves l++ (the left is always shorter); in the decreasing case, every iteration moves r--. Both still O(n).\"",
        "why": "The all-same and monotone cases prove the algorithm's worst-case behavior is still O(n) — every iteration advances exactly one pointer, total iterations = n - 1. The zero-at-one-end case is the one that catches careless implementations that try to skip zero bars; you don't need to skip them, the algorithm handles them naturally. Naming why each edge works (rather than just listing them) shows you've reasoned about the invariant."
      },
      {
        "title": "6. Complexity & wrap-up",
        "prompt": "How would you defend the complexity claim?",
        "say": "\"Time is O(n). The loop runs until `l` and `r` meet. Each iteration advances exactly one of them — `l++` or `r--`, never both, never neither. They start `n-1` apart and meet when `l === r`, so total iterations is exactly `n - 1`. Each iteration does O(1) work — a min, a multiply, a compare, a pointer move. So O(n) overall.\n\nSpace is O(1) — just `l`, `r`, `best`, and the loop-local `h`/`area`. No auxiliary structures.\n\nThe correctness defense is the load-bearing part: I'm pruning n² pairs down to n by always moving the shorter side. The argument is that any pair I skip has the taller side as the bottleneck on the side that I would move, and width strictly shrinks, so the skipped pair is dominated.\n\nIf you wanted a related problem with the same shape: Trapping Rain Water uses the same two-pointer-walk-the-ends template, but instead of an area between two walls, you're accumulating water above each bar bounded by the running max from each side. Same machinery, different accumulator.\"",
        "why": "The O(n) defense is mostly the iteration count, but the correctness defense is what the interviewer is really probing for — without it, you've just described a heuristic that happens to work. Spelling out 'any skipped pair is dominated by one I checked, because width strictly shrinks and height can't exceed the shorter side I moved past' is the rigorous form. The Trapping Rain Water extension shows you see the template — both problems use 'two pointers at the ends, advance the shorter, accumulate.'"
      }
    ]
  },
  "walkthrough": {
    "intro": "Step through the canonical solution one statement at a time on a real input. The highlighted line is what just ran; the state panel shows the locals after that step. Use ◀ Prev / ▶ Next, or pick a different example.",
    "examples": [
      { "label": "[1,8,6,2,5,4,8,3,7] → 49",  "input": [1, 8, 6, 2, 5, 4, 8, 3, 7], "expected": 49 },
      { "label": "[1,1] → 1 (minimal case)",   "input": [1, 1],                     "expected": 1 },
      { "label": "[4,3,2,1,4] → 16 (best at ends)", "input": [4, 3, 2, 1, 4],       "expected": 16 }
    ],
    "trace": [
      "function* trace(input) {",
      "  const height = input.slice();",
      "  yield { line: 1, label: `enter function — height=${JSON.stringify(height)}`, state: { height: [...height] } };",
      "  let l = 0, r = height.length - 1, best = 0;",
      "  yield { line: 3, label: `init l=0, r=${r}, best=0`, state: { height: [...height], l, r, best } };",
      "  while (l < r) {",
      "    yield { line: 4, label: `loop check: l=${l} < r=${r} ? true — enter iteration`, state: { height: [...height], l, r, best } };",
      "    const h = Math.min(height[l], height[r]);",
      "    yield { line: 6, label: `h = min(height[${l}]=${height[l]}, height[${r}]=${height[r]}) = ${h}`, state: { height: [...height], l, r, best, h } };",
      "    const area = h * (r - l);",
      "    yield { line: 7, label: `area = ${h} * (${r} - ${l}) = ${area}`, state: { height: [...height], l, r, best, h, area } };",
      "    if (area > best) {",
      "      best = area;",
      "      yield { line: 8, label: `area=${area} > prev_best → best=${best}`, state: { height: [...height], l, r, best, h, area } };",
      "    } else {",
      "      yield { line: 8, label: `area=${area} ≤ best=${best} → keep best`, state: { height: [...height], l, r, best, h, area } };",
      "    }",
      "    if (height[l] < height[r]) {",
      "      l++;",
      "      yield { line: 10, label: `height[${l - 1}]=${height[l - 1]} < height[${r}]=${height[r]} → l++ (move shorter) → l=${l}`, state: { height: [...height], l, r, best } };",
      "    } else {",
      "      r--;",
      "      yield { line: 11, label: `height[${l}]=${height[l]} ≥ height[${r + 1}]=${height[r + 1]} → r-- (move shorter/equal) → r=${r}`, state: { height: [...height], l, r, best } };",
      "    }",
      "  }",
      "  yield { line: 4, label: `loop check: l=${l} < r=${r} ? false — exit loop`, state: { height: [...height], l, r, best } };",
      "  yield { line: 13, label: `return ${best}`, state: { height: [...height], l, r, best, returns: best } };",
      "}"
    ]
  },
  "reference": {
    "code": "function maxArea(height) {\n  // Start at the widest possible base; we'll trade width for taller walls.\n  let l = 0, r = height.length - 1, best = 0;\n  while (l < r) {\n    // Water bounded by the SHORTER wall.\n    const h = Math.min(height[l], height[r]);\n    const area = h * (r - l);\n    if (area > best) best = area;\n    // Move the shorter side — moving the taller one only shrinks width with no upside.\n    if (height[l] < height[r]) l++;\n    else r--;\n  }\n  return best;\n}\n// Time O(n), Space O(1)",
    "notes": [
      "Water level is bounded by the SHORTER wall — moving the taller side cannot help; it only shrinks width.",
      "Area = min(height[l], height[r]) * (r - l).",
      "Use `<` (not `<=`) — when l === r the width is 0."
    ]
  },
  "L1": {
    "questions": [
      {
        "q": "Why move the SHORTER side inward instead of the taller?",
        "options": [
          "Style choice",
          "Water is bounded by the shorter wall; moving the taller side only shrinks width",
          "It is faster",
          "It avoids overflow"
        ],
        "answer": 1
      },
      {
        "q": "Time complexity?",
        "options": [
          "O(log n)",
          "O(n)",
          "O(n log n)",
          "O(n^2)"
        ],
        "answer": 1
      },
      {
        "q": "Area at each step is computed as:",
        "options": [
          "max(h[l], h[r]) * (r - l)",
          "min(h[l], h[r]) * (r - l)",
          "(h[l] + h[r]) * (r - l)",
          "h[l] * h[r]"
        ],
        "answer": 1
      },
      {
        "q": "Why `while (l < r)` and not `l <= r`?",
        "options": [
          "Performance",
          "When l === r width is 0 — no container to measure",
          "JavaScript requires it",
          "Avoids an off-by-one"
        ],
        "answer": 1
      }
    ]
  },
  "L2": {
    "exercises": [
      {
        "prompt": "Fill in the two-pointer max-area solution.",
        "template": "function maxArea(height) {\n  let l = 0, r = height.length - 1, best = 0;\n  while (l ___ r) {\n    const h = Math.___(height[l], height[r]);\n    const area = h * (r - l);\n    if (area > best) best = area;\n    if (height[l] < height[r]) l___;\n    else r___;\n  }\n  return best;\n}\nconsole.log(maxArea([1,8,6,2,5,4,8,3,7]));",
        "blanks": [
          {
            "answer": "<",
            "hint": "pointers have not crossed"
          },
          {
            "answer": "min",
            "hint": "water bounded by shorter side"
          },
          {
            "answer": "++",
            "hint": "left was shorter — move it inward"
          },
          {
            "answer": "--",
            "hint": "right was shorter or equal — move it inward"
          }
        ],
        "expectedOutput": "49"
      }
    ]
  },
  "L3": {
    "prompt": "Write `maxArea(height)` returning the largest rectangular water area between two vertical lines. Log `maxArea([1,8,6,2,5,4,8,3,7])`.",
    "expectedOutput": "49",
    "canonical": "function maxArea(height) {\n  // Start at the widest possible base; we'll trade width for taller walls.\n  let l = 0, r = height.length - 1, best = 0;\n  while (l < r) {\n    // Water bounded by the SHORTER wall.\n    const h = Math.min(height[l], height[r]);\n    const area = h * (r - l);\n    if (area > best) best = area;\n    // Move the shorter side — moving the taller one only shrinks width with no upside.\n    if (height[l] < height[r]) l++;\n    else r--;\n  }\n  return best;\n}\nconsole.log(maxArea([1,8,6,2,5,4,8,3,7]));",
    "hints": [
      "Two pointers at the ends, walk inward.",
      "Always move the shorter side."
    ]
  }
}
