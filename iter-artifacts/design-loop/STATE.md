# State Ledger

The single source of "where are we." **Read at the start of every iteration;
update at the end.** Keep it short — it's a control surface, not a diary.

---

## Current status
- **Phase:** P4/P5/P6/P8 complete; **P7 drill screens IN PROGRESS** (L1 options → ds done; L2/L3/Reference/Mock/Conv-Walk + shared lesson header remain); P1 nav shell structurally complete (bar + rail = D01)
- **Overall progress:** 6 / 11 phases complete (P0, P1-nav, P4, P5, P6, P8), P7 ◐ (L1 options), P2 ~⅔, P3 done-enough
- **Decided:** nav = **adaptive rail ↔ bottom bar** (D01); theming = **dark-first, light-ready** (D02); visual = **Ink & Amber / minimal** (D03); isolated **`ds/` layer** (D04); inventory pass = **zero capability retirements, chrome-only replacement** (D05); **desktop rail replaces sidebar+dropdowns** (D08); **Progress = one ds surface** (D09); **Browse owns the power filters, drawer retired** (D10); **Settings = one ds sheet, legacy dropdown + desktop icon strip retire** (D11).
- **Last slice shipped:** **P6 — ds Settings sheet (D11)**. New `js/app/21-settings.js` `openSettings()` renders `#settings-sheet` (a `.ds-scrim`/`.ds-sheet` overlay, bottom sheet ≤639px / centered panel ≥640px — presentation identical to the Practice launcher). Six groups: Display (text-size M/L/XL `.ds-seg` · ADHD mode · pace bar) · Feedback (haptics, self-gated on `navigator.vibrate`) · Interview rituals (clarify / hot-seat / calibrate) · Data & sync (sync status→existing sync modal · backup · restore · reset[danger badge]) · Install & offline (both self-gate on capability) · Help (keyboard shortcuts). Each toggle row synth-clicks its hidden `<id>-btn` (D05) → NO new saveProgress field, NO sync-registry change; sheet re-renders to reflect the switch. New `.ds-switch` + `.ds-seg` primitives in ds/components.css; 10 settings icons added to ds/icons.js; page layout in new `css/09-ds-settings.css`. Wiring: `#topbar-settings` pulled OUT of `initTopbarDropdowns` hoverTriggers → wired to `openSettings`; `MODE_ROUTE_SURFACE` settings branch calls `openSettings` (+ added settings/backup/restore/offline-pack slugs) so routed toggles OPEN the sheet not silent-flip (nav-audit P2-5 fixed). Desktop (≥768px) topbar 🔍/❓/⚙ strip `display:none` (rail carries Search+Settings, help folds into the sheet + `?` key); mobile keeps the strip (P2-6b). SW v27 + 21-settings.js + 09-ds-settings.css in APP_SHELL. Probes: NEW p6-settings 21/21 (desktop 15 + mobile 6: adjacency/overlay, ≥44px close, no-emoji, toggle-flips-state+persists, segmented font, danger reset, `#/m/` routes open-not-flip, strip retired); p4b-rail 29/29 + p1-nav-smoke 31/31 (rail Settings→sheet assertion + the pre-existing 5th "Design" nav count both fixed); p4c-filters/p5-progress/appsplit-smoke/sync-merge/sync-coverage(65/65)/validator(920/920)/sysdesign(559/0) all green, 0 console errors. Shots: shots/09-p6-settings/.
- **Last P8 slice:** **family unification (D12)**. Merged the retired root `tokens.css` into `ds/tokens.css` as a value-exact "legacy aliases" block (surfaces/text/accent→ds roles; status colors/radii/dracula kept literal), deleted `tokens.css`, repointed index.html + both standalone pages, SW v28. `system-design.html` + `diagnostic.html` migrated onto ds: MC options → `.ds-opt`, CTAs → `.ds-btn`, sd stats modal → `.ds-scrim`/`.ds-sheet`, all inline hex → tokens (only mermaid `themeVariables` literals remain), return links intact. Docs synced (CLAUDE.md + ui-consistency skill). Probes: NEW p8-family 11/11 (both pages, ds wiring + `.ds-opt` drill + ≥44px + return path + 0 console/net errors + no h-scroll); validate-system-design 559/0; appsplit-smoke PASS (0 errors → no tokens.css 404). Shots: shots/10-p8-family/.
- **Next slice (recommended):** **P7 drill screens** (Reference/L1/L2/L3/reveal + Conv/Walk + Mock on ds — the core loop; protect runner semantics, validate-data 920/920 every commit) OR the **deferred backlog** (per-family drill disclosure in the launcher; heatmap-cell sign-off; Problems⇄Reference toggle fate; delete ~600-line legacy Dashboard/sidebar renderers).
- **P8 minor residuals (→ P9 polish):** system-design's page HEADER still uses emoji (📊 Stats, 📇 badge) — D07 chrome to convert to dsIcon in P9; its header right-buttons sit close to the fixed Sync chip (P2-7 padding was only added to diagnostic).
- **Blocked on:** nothing.

## Open decisions
- (none — all resolved; see `DECISIONS.md`)

## Learnings (append tight bullets as you go)
- The app's URL hash (`#/<lesson>/<tab>`) survives reloads and WINS over seeded localStorage on boot — probes that seed state must `history.replaceState(null,'',location.pathname)` before reloading, or the previous run's tab leaks in.
- The offline service worker is cache-first with a versioned precache: ANY change to index.html / css / js/app REQUIRES a CACHE_VERSION bump + adding new files to APP_SHELL, or existing users keep the stale shell. lib.js now auto-neutralizes SW in probes (it also defeated Network.setCacheDisabled).
- `#audio-dock` had inline `bottom:0` that silently beat stylesheet rules — inline positional styles on fixed elements are landmines for the redesign; move them into CSS when touched (done for the dock).
- Synthetic-click wiring must `e.stopPropagation()` on the originating event or document-level close-on-outside handlers instantly close what was just opened.
- Contrarian deferred items: drawer z-order above nav (fix in P4), Practice label collision in interim launcher (dies with P3), programmatic `.is-active`/aria-current (wire in P2).
- This environment blocks CDN domains (cdn.tailwindcss.com/cdnjs/jsdelivr → proxy 403) but allows registry.npmjs.org. Fix shipped: `bash tools/cdp/fetch-vendor.sh` once per container, then lib.js auto-serves vendored bytes via CDP Fetch interception. Without it, screenshots render UNSTYLED — do not trust probe shots if vendor/ is missing.
- `tools/cdp/lib.js ensureChrome` uses macOS `open -na`; on Linux start Chromium manually: `/opt/pw-browsers/chromium --remote-debugging-port=9222 --user-data-dir=/tmp/chrome-debug-jsdrill --headless=new --no-sandbox` (lib then detects it as already-up).
- `npm install --no-save ws` needed once per container for the probes.
- `before-shots.js` takes an outRoot argv — reuse it for the P10 after-sweep (`node tools/cdp/before-shots.js iter-artifacts/design-loop/shots/99-after`).
- Probes sharing one Chrome share one ORIGIN: a later `connect()` inherits the localStorage + lastTab the previous section saved (a desktop run ending on L3 made the next mobile boot hide the nav). Every probe section must seed its own state + strip the hash + reload.
- `offsetParent === null` does NOT detect the off-canvas drawer (it's transformed, not display:none) — gate on `body.classList.contains('sidebar-open')` instead.
- Global rules in css/06-ds-nav.css can silently out-cascade equal-specificity `display:none` rules in 05-shell-chrome (same 0,1,0, later file wins) — the D07 `.topbar-icon` centering rule un-hid the mobile-only topbar icons on desktop for 2 iters. When adding a global rule over legacy chrome classes, grep for `display: none` on the same class.
- P4b deferred (for P6): rail Settings synth-clicks #topbar-settings so the settings dropdown opens anchored top-right (spatially disconnected from the rail foot); topbar 🔍/?/⚙ icon strip is now redundant with the rail foot — P6's Settings surface should resolve both. Drawer box-shadow is a hardcoded rgba mirroring legacy 02-sidebar (drawer dies/restyles in P4 part 3). Reference-tab toggles (Flash/Cinema/Notes→Code) still carry emoji — converts with P7 per D07 rollout.
- The legacy mobile readability bump `#lesson-shell p { font-size: 1rem }` (css/02-sidebar) out-cascades ANY ds class inside a shell page via ID specificity — it now carries `:not(.ds-root p)`, but its siblings (`#lesson-shell .text-xs/.text-sm/.mc-option/...`) will do the same to a ds page that ever reuses those legacy class names. Check the cascade FIRST before suspecting Chrome font-boosting (two wasted guards here before finding the real rule).
- `#/m/<mode>` boot deep-links to shell pages (Today/Browse/Progress) were silently broken: renderLesson's content-cache-miss path schedules an async re-render that fired AFTER the boot mode dispatch and clobbered the page. Fixed with a `[data-lesson-loading]` marker guard (10-render-sidebar-lesson.js) — re-render only if the loading placeholder still owns the shell.
- Probing a boot deep-link: set the hash AFTER `connect()` and reload — lib's SW-neutralize reload lets the first boot normalize the URL to the lesson hash, eating the mode.
- P5 deferred: legacy renderers (renderDailyInto/renderActivityInto/renderStatsInto ≈600 lines) + the at-risk modal are dead code behind delegation — delete when dashboard-probe/dashboard-activity-probe/refine-stats-modal/at-risk-radar probes retire or are rewritten. Bridge (`bridge-btn`, INVENTORY MERGE→Progress) still routes directly — give it a Fix-first row in a later slice. Heatmap day-cells are <44px (informational; title + persistent detail line carry the content — same tradeoff every calendar heatmap makes). At-Risk's old "2 DUE NOW · 1 SOON" inventory strip wasn't reproduced (per-row chips carry the same data).
- P7: to move a mature stateful surface (L1) onto the ds look with minimal risk, RESTYLE the existing class in CSS rather than swap class names — keep `.mc-option`/`.correct`/`.incorrect`/`.disabled`/`.selected` (12a-l1.js's 4 click/replay sites toggle those) and just repoint their CSS to ds tokens + add a `.mc-key` chip in the markup. Zero interaction-logic churn, no runner impact (L1 has no runnable code → validate-data unaffected). BUT changing the option markup's letter format (`A.` → a bare `A` chip) broke probes that extracted option text by stripping an `A.` prefix (l1-shuffle, mistake-tagging); the fix is to read `.mc-body`'s textContent (the clean option text, chip-free). When you restructure MC-option markup, grep probes for `[A-D]\\.` extraction.
- P6: `#topbar-settings` was BOTH a dropdown hover-trigger (in `initTopbarDropdowns`) and the synthetic-click target for the rail/routes. To repurpose it as the Settings-sheet launcher, it must be pulled OUT of `hoverTriggers` (else BOTH the legacy dropdown and the sheet open on one click). The document-level outside-close `if (e.target.id === 'topbar-settings') return;` guard is now vestigial but harmless.
- P6: settings toggles are backed by hidden `<id>-btn`s whose OWN handlers flip `state`, `saveProgress()`, and re-render the lesson if needed — so the sheet just synth-clicks them (D05) and re-renders its body. Zero new state, zero sync-registry churn (`check-sync-coverage` stays 65/65). `#font-size-btn` only CYCLES md→lg→xl→md, so to SET a specific size from the segmented control, click it in a ≤4-iteration guard loop until `state.fontScale` matches.
- P6: DON'T click a destructive row (`#reset-btn`) in a probe — its `confirm()` blocks the CDP session (browser modal dialogs freeze all further events). Assert the row's PRESENCE + danger styling instead.
- P6/regression: two nav probes (p4b-rail, p1-nav-smoke) asserted **4** bottom-bar destinations but a prior merged commit (079223d) added a 5th ("Design"/System Design) — they were already RED on origin/main before this slice. Fixed the counts to 5 (7 rail navitems incl. 2 aux). When a probe fails, first `git diff HEAD -- <probe>` to see if the failure predates your slice.
- P4c deferred: (1) legacy probes that drove the DRAWER UI (tag-filter.js, per-track-starter-path.js, path-aware-sr.js, refine-plan-picker.js, section-progress-bar.js…) weren't run — their synthetic clicks still work (handlers + hidden DOM remain) but any drawer-VISIBILITY assertion will fail; rewrite against Browse or retire them with the renderSidebar cleanup. (2) renderSidebar still paints the hidden aside on every save — dead work kept for reversibility; delete alongside those probes. (3) The Topic facet is ~19 wrapping 44px chips — fine inside the closed-by-default panel, but a per-group disclosure would be tighter craft. (4) The topbar Problems⇄Reference toggle's main effect was the sidebar; with the drawer gone it only steers Browse segment sync + lesson context — revisit its place in P6/P7. (5) Browse search text is deliberately NOT persisted (fresh page starts clean, D10) while the dead `state.searchQuery` field lingers for the hidden sidebar — unify when renderSidebar goes.

## Guardrail reminders
- Every iteration: independently green (`node tools/validate-data.js`), browser-tested @390px + desktop, committed per convention.
- Preserve capability; log any retirement in `DECISIONS.md`.
- Update `ROADMAP.md` statuses when a slice lands; drop a before/after pair in `shots/`.

---

### Iteration log (newest first — one line each)
<!-- YYYY-MM-DD · iter N · phase · slice shipped · rubric delta · shots ref -->
- 2026-07-11 · iter 18 · backlog closeout (docs) · D14 — explicit disposition of every deferred item (DONE: P2-4 disclosure / D13 toggle / mistake-tagging probe / Bridge+At-Risk rows already in Progress; ACCEPTED tradeoffs: heatmap <44px cells + At-Risk summary strip; DEFERRED with reason: legacy-renderer deletion [needs probe retirement], topic-facet density, Reference/system-design/L1 emoji [holistic P7], weakness-clear timestamps [sync-correctness pass]) · backlog now auditable · n/a
- 2026-07-11 · iter 17 · P7 · L2/L3 chrome polish (L3 Hint 💡→ds lightbulb icon + Critical-lines 🎯→ds target icon [D07; buttons are inline-flex ds now so icons align]; L2+L3 preambles trimmed to one line; SW v33; p7-buttons 6/6 + validate-data 920/920 + l3-hint-ladder/critical-lines green) · craft ↑ (no emoji in the L3 action bar) · Time-respect ↑ (tighter preambles) · shots: 13-p7-buttons refreshed
- 2026-07-11 · iter 16 · P7 · app-wide action buttons → ds-btn treatment (every button.primary/.secondary across L1/L2/L3/drills/mock: ds tokens + ds radius + ≥44px tap target [were ~33px]; CSS-only restyle, class names kept → handlers/probes intact; font-size held 13px → zero horizontal reflow, buttons only grow taller+rounder; SW v32; p7-buttons 6/6 + appsplit + refine-l2 green) · Phone-fit ↑↑ (≥44px targets app-wide) · consistency ↑↑ (one button language) · shots/13-p7-buttons/
- 2026-07-10 · iter 15 · backlog (D13, user decision) · Problems⇄Reference toggle folds into Browse at desktop (retired ≥768px; kept on mobile as the lesson track-switcher until P7); DOM+handler stay (D05); SW v31; p4b-rail 30/30 (+toggle-folded) + p1-nav-smoke 32/32 (+toggle-on-mobile) · Decisions ↑ (desktop chrome de-cluttered) · shots: (none — CSS-only)
- 2026-07-10 · iter 14 · backlog (P2-4) · Practice-launcher per-family drill disclosure (▾ expands the member list inline; family row still random-shuffles; members synth-click their data-btn-id; zero state; SW v30; p2-4-family-disclosure 9/9) · Decisions ↑ (specific drill launchable without palette typing — ADHD user) · Autopilot ↔ (shuffle default preserved) · shots/12-launcher-family/
- 2026-07-10 · iter 13 · P7 · L1 options → ds MC-option look (letter chip + 2px border + wash reveal + mute-on-lock; .mc-option restyled to ds tokens with state class names UNCHANGED → click/lock logic byte-identical, no runner risk; preamble trimmed; SW v29; p7-l1 6/6 + l1-shuffle 12/12 + mistake-tagging fully green [also updated its P5-stale Stats→Progress assertions] + l1-orange/tab-switch green; validate-data 920/920) · core-loop consistency ↑ (L1 now matches the family MC surface) · Phone-fit ↑ (tighter preamble → faster to Q1) · Time-respect ↑ · shots/11-p7-l1/
- 2026-07-10 · iter 12 · P8 · family unification (D12 — system-design.html + diagnostic.html onto ds/tokens.css + ds/components.css: MC options → .ds-opt, CTAs → .ds-btn, sd modal → ds-scrim/ds-sheet, inline hex → tokens; legacy tokens.css merged into ds/tokens.css as value-exact aliases + deleted; SW v28; p8-family 11/11 + validate-system-design 559/0 + appsplit PASS) · one design system across ALL pages (VISION row 4) · consistency ↑↑ · shots/10-p8-family/
- 2026-07-10 · iter 11 · P6 · ds Settings sheet (D11 — one grouped overlay: Display/Feedback/Rituals/Data&sync/Install&offline/Help; `.ds-switch`+`.ds-seg` primitives; toggles synth-click hidden btns → no new state; `#topbar-settings`→openSettings, `#/m/` toggle routes open-not-flip, desktop 🔍/❓/⚙ strip retired; SW v27; p6-settings 21/21 + all regression green) · Decisions ↑↑ (1 grouped surface vs a scattered icon strip + 1,199px dropdown) · ADHD-fit ↑ (adjacent focused overlay, one focus) · Phone-fit ↑ (44px rows, bottom sheet, close affordance) · shots/09-p6-settings/
- 2026-07-10 · iter 10 · P4c · Browse power filters as first-class ds controls (Plan view + scope chips / Hide mastered / Needs work / facets / plan switcher in one disclosure; ranked attention chips on rows; IME-safe search) + off-canvas drawer RETIRED (D10; `/`→Browse-search-or-palette; hamburger→Browse redirect; SW v23; p4c-filters 36/36 + updated p4b-rail/p1-nav-smoke + p5-progress/appsplit green) · Decisions ↑↑ (one filter surface + one visual language vs page+drawer split) · Phone-fit ↑ (44px chips vs drawer micro-chips) · ADHD-fit ↑ (no off-canvas context switch) · shots/08-p4c-filters/
- 2026-07-10 · iter 9 · P5 · Progress page (ds rebuild of Dashboard: snapshot→activity→fix-first→mastery→insights-disclosure; At-Risk modal absorbed, Resurrect/Reveal rows, heatstrip summary; D09; `#/m/` boot-clobber + `#lesson-shell p` cascade fixes; 31/31 + regression probes green) · Progress-visible ↑↑ (one calm arc vs kitchen-sink) · Decisions ↑ (8 tiles → 1 disclosure) · ADHD-fit ↑ · shots/07-p5-progress/
- 2026-07-10 · iter 8 · P4b · Desktop rail (≥768 rail + Search/Settings foot; dropdowns+Dashboard link retired; sidebar→drawer; 768 unified; `/`→palette fallback; System Design→palette; 35/35 + 33/33; self-review, 2 topbar-leak fixes) · Decisions ↑↑ (desktop: 4 calm destinations vs 6-menu topbar) · ADHD-fit ↑ (S2 at-desk) · shots/06-p4b-rail/
- 2026-07-10 · iter 6 · D07 icons (user-directed) · ds/icons.js stroke set replaces emoji in nav/launcher/topbar/home; no-emoji probe gate · craft bar ↑ (Linear-tier chrome) · shots/04-p3-launcher/ (refreshed)
- 2026-07-10 · iter 5 · P3 · Practice launcher bottom sheet (taxonomy-derived, 4 groups, 26/26 probe; contrarian PASS) · Decisions ↑ (grouped disclosure at the thumb) · ADHD-fit ↑ (no more top-menu eye-jump) · shots/04-p3-launcher/
- 2026-07-10 · iter 4 · P2 · Today home (hero next-rep + streak grace + stat tiles + THEN; mock-C-faithful; contrarian BLOCK fixed) · Autopilot ↑↑ (J1 = 1 tap) · Progress-visible ↑ (ambient tiles/streak) · shots/03-p2-home/
- 2026-07-10 · iter 3 · D06 retheme · global Ink & Amber sweep (~1,190 lines + Tailwind remap + contrast fixes; user-directed acceleration) · whole family visually transformed · shots/02-retheme/
- 2026-07-10 · iter 2 · P1 · mobile bottom nav (4 tabs, ds/ layer, L3-immersive, dock-lift; contrarian BLOCK on dock fixed) · Phone-fit ↑ (thumb bar vs corner icons) · Decisions ↑ (4 labeled destinations vs 3 unlabeled icons + hamburger) · shots/01-p1-nav/
- 2026-07-10 · iter 1 · P0 · before-shots (35) + INVENTORY verdicts (D05) + sandbox CDN-vendor probe infra · baseline captured · shots/00-before/
- 2026-07-10 · iter 0 · P0 · ds/tokens.css + ds/components.css v1 + gallery; D01–D04 locked · n/a · —
